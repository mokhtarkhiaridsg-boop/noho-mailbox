# Loop Roadmap — Remarkable Core Features

Each `/loop` iteration pulls one item from this list, ships it as a substantial feature (not a polish), and the next iteration learns from the previous (audit-trail patterns get reused, new components get factored, every send gets logged).

Built so far through iter-83 lays down the operational chassis — intake, pickup, dropoff, audit, notifications, exports, lookups, QR pickup, bulk mailer. Everything below builds on that.

---

## Tier 1 · Communication & growth (ship next)

1. **SMS notifications via Twilio** — mail-arrived + picked-up + storage-warning texts. Per-customer opt-in preference. Reuses the Notification + EmailLog patterns; new `SmsLog` table.
2. **Customer notification-preference center** — UI on member settings to toggle email vs SMS vs in-app per event type. Backed by a JSON `notifPrefs` blob on User (already exists in schema).
3. **Annual statement PDF** — per-customer PDF of all activity (intake, pickups, fees, payments) with a year-end summary. Generates on-demand from member dashboard, archived to Vercel Blob.
4. **Customer-satisfaction NPS survey** — fires after pickup-confirm email, simple 1–5 stars + comment. Aggregated on a new admin `Insights` page.
5. **Public package-tracking page** without login (`/p/[id]?token=`). Customer can share with sender; shows photo + status timeline + ETA. Token-gated so it can't be enumerated.

## Tier 2 · Money & billing

6. **Auto-bill storage fees on pickup** — when admin marks Picked Up, if storage was active, charge wallet (or generate invoice). Uses the existing wallet/invoice infrastructure.
7. **Stripe / Square recurring billing** for plan renewals. Card-on-file auto-charges 7 days before plan due date.
8. **POS sale completion + thermal receipt** — finish wiring `pos.ts` so AdminPOSPanel can ring up cash/card/wallet sales with a printable receipt. Blocked by the empty pos.ts Turbopack hiccup; resolve and ship.
9. **Bookkeeping export** — monthly QuickBooks IIF + plain-CSV export of all paid invoices, walletTransactions, POS sales, mailbox renewals, dropoff fees.
10. **Storage-fee dispute workflow** — customer disputes from dashboard, admin sees in a new "Pending disputes" queue, can waive or uphold with audit trail.

## Tier 3 · Operations & power features

11. **Cmd+K admin omnibox** — global jump-to-anything: customer name → their page; tracking # → lookup result; "log dropoff" → opens form. Mirrors the member CommandPalette that just shipped.
12. **Daily KPI digest email** to admin every morning — yesterday's intake / pickups / dropoffs / revenue / churn signals.
13. **Slack/Discord webhook bridge** — every package-arrived event, every dropoff, every storage-tier breach posts to a configurable webhook URL.
14. **Two-factor auth for admin sessions** — TOTP. The `totpSecret` + `totpEnabled` columns already exist; build the enrollment + verify flow.
15. **Operating-hours config** — admin-editable holiday + special-hours table. Auto-shows on marketing site + locks customer-facing actions during closed hours.
16. **Mailbox key audit ledger** — full who-has-key-N timeline (assignments, returns, replacements). Backed by the existing MailboxKey model + audit log.

## Tier 4 · Customer self-serve

17. **Pickup-time scheduling** — customer books a 15-min slot, admin sees a sorted queue. Backed by a new `PickupAppointment` table.
18. **Guest-pickup authorization with QR** — customer authorizes a guest from their dashboard; guest gets a one-time QR; admin scans it to release a specific package. Backed by existing `GuestPickupAuth` model.
19. **Shared-mailbox access** — primary user grants secondary access to view their packages. Backed by existing `SharedMailboxAccess` model.
20. **Vacation-hold workflow** — customer schedules a hold window from their dashboard; admin sees it on the shelf list; auto-resumes on end date. Backed by existing `VacationHold` model.

## Tier 5 · Carrier & compliance

21. **USPS / UPS / FedEx live tracking** — auto-poll carrier APIs for the active scans; surface "package out for delivery", "exception", "delivered" events on the member dashboard with timestamps.
22. **AI photo analysis on intake** — call Claude Vision on every photo upload to detect carrier label, fragile/this-side-up stickers, suspected hazardous content. Surfaces warnings on the row.
23. **CMRA quarterly compliance report** — auto-generates the USPS Form 1583 quarterly customer roster + change report.
24. **ID-document expiry alerts** — when `idPrimaryExpDate` is <90d away, fire a renewal-prompt email + dashboard banner.
25. **Insurance / declared-value workflow** — customer can upgrade a label or in-bureau dropoff with declared value; calculated insurance fee adds to the cart.

## Tier 6 · Differentiators

26. **Multi-language support (Arabic + French)** — i18n layer for marketing + dashboard. Ties to the Khiari memory (Tunisia bureau ops).
27. **PWA / offline mode for the bureau scanner** — InboundScanPanel works on a tablet at the counter even with flaky wifi; queues actions locally, syncs on reconnect.
28. **Same-day delivery driver app** (PWA) — driver sees their route, scans for proof-of-pickup, captures POD photo at delivery.
29. **Member referral rewards** — already half-built (Referral model exists). Customer shares a link, both get a credit when referee signs up.
30. **Bulk customer onboarding via CSV** — admin uploads a customer-roster CSV (name, email, suite, plan, KYC status) → preview → import. Invites all in one shot via the new bulk mailer.

---

## Loop discipline

- Every iteration: ship one feature end-to-end (server action + UI + audit log + appropriate notification) — not a polish, not a chip, not a rename.
- Every iteration: extend or reuse the patterns shipped in earlier iters (`prisma.$transaction` for atomic write+audit; `LightboxContext` for any new photo surface; `playBeep` for any new admin scan event; `wrapLayout` for any new email).
- Every iteration: build verify → exit 0 → schedule the next.
- Every iteration learns: if something broke (e.g. iter-82 hit a Turbopack stale cache), document the workaround so future iters don't trip on it again.
- HANDOFF.md gets a §N append every 5 iters covering the block.

When all 30 ship, the bureau has the most opinionated mailbox-store SaaS on the market.

---

## Tier 7 · Post-roadmap (added at iter-112)

All 30 original items shipped iter-83 → iter-111. Tier 7 (#31-#45) shipped iter-112 → iter-132.
All 45 features shipped end-to-end. Loop continues with Tier 8 ideation (#46+).

New ideas to keep the loop going:

31. **Wallet auto top-up** ✓ shipped iter-112 — when wallet drops below member-set threshold, auto-creates a CreditRequest and emails them.
32. **Holiday closure auto-emails** ✓ shipped iter-113 — daily cron emails active customers 2 days before each iter-90 holiday.
33. **Member onboarding checklist** ✓ shipped iter-114 — visual progress arc + 8 items + per-item Go links + auto-collapse to chip when 100% done.
34. **Mailbox upgrade flow with prorate** ✓ shipped iter-116 — Basic→Business→Premium upgrade card with per-option prorate calc, wallet-funded charge, atomic plan switch + MailboxRenewal record + audit + receipt email + admin webhook.
35. **Holiday-aware email scheduler** ✓ shipped iter-130 — `DeferredEmail` queue + `sendOrDefer()` helper + `decideHolidayDeferral()` lib + drain cron + admin panel with cancel/send-now per row + 9 default-deferred kinds.
36. **Sticky note pinning UI** ✓ shipped iter-120 — global pinned-notes board (sticky-card grid) + customer detail drawer with pin/unpin/delete + create form with kind picker + customer search.
37. **Saved package contacts** ✓ shipped iter-119 — per-customer recipient autocomplete chips on the intake panel, frequency-weighted with recency boost, click-to-fill.
38. **Customer photo album** ✓ shipped iter-115 — paginated grid of every package + scanned-mail photo for the member, with All/Packages/Mail filter chips + click-to-zoom lightbox + lazy loading.
39. **Mailbox suite occupancy heatmap** — admin visual grid of all suites + utilization %.
40. **Bulk forward batch** ✓ shipped iter-127 — pending-forwards queue + per-row checkboxes + per-row address picker + atomic per-item process with audit + roll-up batch audit + failure surfacing for retry.
41. **Affiliate earnings dashboard** ✓ shipped iter-121 — leaderboard with medal ranks + 12-month closed-vs-paid SVG bar chart + payout queue with mark-paid action + 4 stat tiles.
42. **Customer time-zone awareness** ✓ shipped iter-123 — User.timeZone column + member card with 16 common TZs + browser-detect shortcut + custom IANA input + live "now in X" preview + helper for emails to render times in user's TZ.
43. **Backup verification panel** ✓ shipped iter-132 — `runBackupHealthCheck()` runs 5 probes (connectivity SELECT 1, write probe via SiteConfig sentinel round-trip, schema integrity via Promise.allSettled across 12 model counts, write recency with stale thresholds, env-var presence ✓/✗ never values) + admin panel auto-runs on mount with gradient overall-status banner + 5 check tiles + row-counts grid + recency timeline + env config matrix + last-10-runs history. Audit-logged as `backup.health_check_ran`.
44. **Member self-service mailbox transfer** ✓ shipped iter-122 — schema + vacant-suite picker + reason form + admin queue with race-safe approval (atomic User.suiteNumber swap + audit) + decision emails + webhook.
45. **Carrier wait-time predictor** ✓ shipped iter-126 — per-customer avg/median/fastest/slowest pickup days mined from audit log, "late" flag at >50% over their own pace, sparkbar visualization, predicted-pickup-by date for in-progress packages.

---

## Tier 8 · Post-Tier-7 (added at iter-133)

Tier 7 closes at iter-132 with #43 backup-verification panel — all 15 Tier 7 items shipped. New backlog of remarkable bureau features to keep the loop going.

**iter-146 (mid-Tier-9 redesign)** — Label printer redesigned to mirror the Shippo USPS Ground Advantage layout (the carrier-industry-standard format every depot scans without thinking) but with NOHO branding throughout. Sections from top: header (NOHO logo block + service-class info: "USPS GROUND ADVANTAGE / U.S. POSTAGE PAID / NOHO Mailbox / [carrier] Ship"), big service banner, sender block + ship-date/weight/REF column, dominant TO block (datamatrix placeholder + recipient name in 0.20in / customer-name c/o line / dimensions / email), pure-white tracking band (CARRIER TRACKING # header + Code 128 + space-formatted human-readable number both above AND below), MARKETING footer band (logo + "NOHO MAILBOX" wordmark + tagline "Real street address · package handling · mail forwarding" + nohomailbox.org · phone + "Retain Until Pickup" badge). Side accent strips (NOHO blue + cream split, 0.06in wide) on far left/right edges only — center stays pure white per USPS scannability rules. Service-label string maps per detected carrier (USPS→GROUND ADVANTAGE, UPS→GROUND, FedEx→HOME DELIVERY, DHL→EXPRESS, default→STANDARD). Drives sales/marketing every time a recipient or driver looks at one of our packages.

**iter-136 (mid-Tier-8 fix)** — Label printer pivoted from DB-only to ONLINE carrier lookup. `findLabelByTracking` now auto-detects carrier from the tracking pattern, hits Shippo's tracking API in parallel with the local DB lookup (Promise.all), returns `OnlineTracking { carrier, status, location, etaIso, history[], source }`. Panel renders an `OnlineTrackingPane` directly under the tracking input: live status pill (PRE_TRANSIT/TRANSIT/DELIVERED/RETURNED/FAILURE), location, ETA, "✓ matches our intake" overlay when DB also has the row, and the last 4 carrier scan rows. Falls back to detect-only chip when Shippo isn't configured. Audit-logs every `labelprinter.online_lookup`. Source field on LabelData expanded to `"online" | "db+online" | "manual"`.

46. **Customer credit / wallet ledger PDF** ✓ shipped iter-133 — `getWalletLedger()` server action returns full lifetime transaction history with computed opening/closing/credited/debited summary + entry list (DESC, includes invoiceId). Printable `/dashboard/wallet/ledger` page renders branded SSR statement: hero (member, suite, period), 4-tile summary, full table with kind pill + signed amount + running balance column, footer disclaimer. "Download statement" pill in WalletPanel opens it in a new tab. Audit-logged as `wallet.ledger_viewed`.
47. **Webhook delivery retry & dead-letter** ✓ shipped iter-134 — `WebhookDelivery` schema gained `attempt`/`nextRetryAt`/`deadLettered`/`lastTriedAt`. Failed deliveries auto-schedule retry on exponential backoff (60s/5m/30m/2h/12h, 6 total attempts) → after MAX dead-lettered. New `/api/cron/retry-webhooks` (Bearer-auth) drains due retries via `drainWebhookRetries()` (capped 100/tick). Admin panel gained "Pending retries · Dead letters" section with per-row Replay (resets attempt, re-fires, audit-logs `webhook.replayed`) and Discard (dead-letter only, audit-logs `webhook.deadletter_discarded`). Failed/pending/dead-lettered rows are excluded from the 200-per-endpoint trim so admin always has visibility.
48. **Multi-photo intake gallery** ✓ shipped iter-135 — `MailItem.extraPhotosJson` JSON column (capped 10) holds `{id, url, label?, addedAt}` extras. Server actions `addExtraPhoto`/`removeExtraPhoto`/`relabelExtraPhoto`/`listExtraPhotos`/`getMailItemGallery` are atomic + audit-logged. Member `PackagesPanel` shows primary photo with `+N` count badge; thumb strip below row with admin-set labels; click any thumb opens self-contained `PhotoLightbox` swiper (←/→ keys, ESC, backdrop close, dot indicators, caption). Admin `AdminMailItemPhotosModal` opens from per-row camera icon: existing primary read-only, extras editable inline (rename, remove), URL paste OR file upload via `/api/upload`, 6 preset label chips (Back/Label close-up/Contents/Damage/Side/Top).
49. **Pickup signature capture** ✓ shipped iter-137 — `MailItem` gained `pickupSignatureSvg`/`pickupSignerName`/`pickupSignedAt`. New `<PickupSignatureModal>` is a touch-friendly pointer-events-backed SVG pad: captures polylines as compact path-data (1-decimal precision, 0.7px point dedup), exports as a self-contained SVG document. Server actions `recordPickupSignature`/`clearPickupSignature`/`getPickupSignature` validate (50 KB cap, no markup chars) + atomic write + audit-log (`mail.pickup_signature_captured`/`_cleared`). New ✍️ icon on every `MailItemCard` opens the pad; on save the modal also fires `handleMailAction(id, "Picked Up")` so the existing receipt-email + SMS + webhook + storage-fee pipeline runs unchanged. `sendMailPickedUpEmail` re-fetches the row + embeds the signature SVG inline beside the typed signer name.
50. **Invoice editing & adjustments** ✓ shipped iter-138 — `InvoiceMeta` extended with `adjustments[]` (kind: discount/waiver/surcharge, signedCents, customer-visible description, internal reason, byActorId, atIso, voidedAt). `computeInvoiceTotals` returns new `adjustmentsCents` + `totalBeforeAdjustments`; final total clamped to ≥0. Server actions `addInvoiceAdjustment`/`voidInvoiceAdjustment`/`listInvoiceAdjustments` enforce: positive amount, ≥2-char description AND reason, blocks Paid/Void invoices, atomic recompute of `Invoice.totalCents`, audit-logs `invoice.adjustment_added`/`_voided`. Voids stay in array with `voidedAt` for permanent audit. New `<InvoiceAdjustmentsModal>` (3 kind chips, $ + description + reason fields, totals strip, active list with Void buttons, voided history). `<AdminInvoiceBuilder>` gained "Adjust" button with active-count badge. Printable invoice page renders signed adjustment rows between subtotal/tax and total — green for credits, amber for surcharges.
51. **Smart routing for inbound mail** ✓ shipped iter-139 — `routeRecipientName({ recipient })` server action tokenizes (handles "Doe, John" / titles / suffixes / business aliases), pulls 60-row candidate pool via OR-contains across name/email/businessName, scores on 7 axes: surname × 60 + first-name × 25 + residue × 15, business-name-in-haystack +50, email-prefix +8, recency bonus (mail-items in past 30d, +3 to +15), Active-mailbox +5. Lev-distance fallback (≤1 short, ≤2 long) catches typos. Returns top 5 + `autoPick` when score ≥ 85 AND second place ≥ 25 behind. Audit-logs `intake.smart_routed`. `AdminInboundScanPanel` shows ✨ "Smart match" banner above customer-search results when admin types a 2+-word recipient name; top chip is GREEN when auto-pickable. One click pre-fills suite + recipient name with the typed envelope text.
52. **Customer health score** ✓ shipped iter-140 — `getCustomerHealthScores({userIds?})` computes a 7-axis signed score per member from existing tables (no new schema): tenure (0→+20 over 36mo cap), payment punctuality (-25→+25, % paid-on-time over last 12mo invoices), overdue invoices (-25, 8/each), open disputes (-15, 5/each), pickup-survey satisfaction (0→+15, avg ★/5 × 15), 90-day engagement (0→+10), standing (KYC +5, auto-topup +3, 2FA +2). Raw -65..+95 normalized to 0-100; bucket Excellent/Healthy/Watch/At Risk. All aggregations batched via `Promise.all` + `groupBy` so list of 300 customers = 4 queries total. New `<CustomerHealthBadge>` shows colored pill that pops a per-axis breakdown popover (signed contribution per axis + human-readable detail). `AdminCustomersPanel` now has sortable Health column in table view, badge inline in card view, "Health watch" segment chip filtering Watch + At Risk.
53. **Auto-reply rules for mailer** ✓ shipped iter-141 — new `MailerAutoReplyRule` schema (label, matchOn `any|subject|body`, JSON keywords, replySubject/replyBodyHtml templates, cooldownHours, businessHours `any|open|closed`, sendCount). `MailerMessage.autoReplyRuleId` tracks which rule fired each outbound row. `matchInboundAgainstRules()` runs from `ingestInboundEmail` (fire-and-forget) — case-insensitive contains match, business-hours gate via SiteConfig.operatingHours, per-rule per-customer cooldown enforced by querying `MailerMessage` with `autoReplyRuleId+toEmail+sentAt`, template substitution for `{customerName}`/`{firstName}`/`{originalSubject}`/`{firstLine}`, atomic `prisma.$transaction([msg.create, thread.update, rule.update, audit])`, audit-logs `mailer.auto_reply_fired`. Admin CRUD: `listAutoReplyRules`/`upsertAutoReplyRule`/`deleteAutoReplyRule`/`toggleAutoReplyRuleActive`. New `AdminMailerAutoReplyPanel` with 3 presets (vacation/after-hours/pricing), live keyword chips, fire-counter + last-fired display, business-hours selector, full editor with template placeholder hints.
54. **Storage tier auto-graduation** ✓ shipped iter-142 — graduated rate ladder ($6.50 → $9.75 → $13.00 → $19.50/day at days 4/14/30/60); `tieredStorageFeeCents(days)` is the single source of truth used by both the at-pickup charge AND the threshold-alert email previews. New `StorageThresholdAlert` schema (`mailItemId × threshold` unique with cascade-on-pickup) provides per-(item, threshold) idempotency. `runStorageTierSweep()` scans every Package on shelf >14d, sends ONE escalating email per threshold (early/mid/final tone, color-coded with current fee + next-tier preview), atomic `prisma.$transaction([alert.create, audit])`, fires `storage.threshold_alert` webhook. New `/api/cron/storage-tier-sweep` (Bearer-auth, capped 500/tick). `applyStorageFeeOnPickup` now delegates to the tiered math so the charge matches what the customer was told. Bonus: fixed pre-existing `SquareStatus.environment` type holes in 3 spots — tsc fully clean.
55. **Suite cleaning / maintenance log** ✓ shipped iter-143 — new `SuiteMaintenanceLog` schema (suiteNumber, kind: cleaned/inspected/repaired/lock_changed/other, notes, performedById, performedAt). Server actions `logSuiteMaintenance`/`deleteSuiteMaintenance`/`listSuiteMaintenanceForSuite`/`getSuiteMaintenanceOverview` (single batched query computes last-per-kind + per-suite totals in JS, no N+1). Status thresholds: cleaning due-soon at 21d / overdue at 30d, inspection due-soon at 60d / overdue at 90d. New `AdminSuiteMaintenancePanel` with 5 status filter chips (live counts per tier), sortable table (Suite/Customer/Cleaning/Inspection/Logs), per-row drawer with "Log new entry" form (5 kind chips + notes textarea + Save) and full history with delete-with-audit. All writes audit-logged as `suite.maintenance_<kind>` / `suite.maintenance_deleted`. Wired into Operations nav.

---

**Tier 8 closes at iter-143 — all 10 items shipped (iters 133-143).**

## Tier 9 · Operations depth (added at iter-143)

56. **Equipment / scanner inventory** ✓ shipped iter-145 — new `Equipment` schema (serial unique, category enum, location, assignedToUserId, status `active|needs_service|retired|lost`, vendor/model/purchasedAt/purchasePriceCents/warrantyEndsAt/notes) + `EquipmentServiceLog` (kind, costCents, performedAt, notes). Server actions `listEquipment`/`upsertEquipment`/`deleteEquipment`/`logEquipmentService`/`deleteEquipmentService`/`listEquipmentServiceLog` — all atomic + audit-logged (`equipment.created`/`updated`/`deleted`/`service_<kind>`). Auto-clears `needs_service` when admin logs a service/repair/calibration. Freshness derived from last-service age (fresh ≤30d, ok 30-180d, stale ≥180d, never). New `AdminEquipmentPanel` with category + status filter chips (live counts), sortable table (Device + emoji / Serial / Location → assignedTo / Status / Last service freshness), per-row Edit modal with all fields + delete-with-confirm, click-to-open service-log drawer with inline "Log new event" form (7 service kinds + cost field) and full history with delete-with-audit. Wired into Operations nav.
57. **Door-access keypad codes** ✓ shipped iter-147 — new `DoorAccessCode` (userId, 4-digit code, issuedAt/rotateAt/retiredAt, createdById/retiredById, notes) + `DoorAccessEntry` (codeId nullable, enteredCode, result `granted|denied|denied_retired|denied_unknown`, attemptedAt). Active-only uniqueness enforced in JS via collision-retry generator (10K possible codes, ~1 try expected). Server actions `listActiveDoorCodes`/`listRecentDoorEntries`/`issueDoorCode`/`retireDoorCode`/`rotateDoorCode`/`checkDoorCode`. `issueDoorCode` retires existing active code in same `prisma.$transaction`, audit-logs `door.code_issued`, fires `door.code_issued` webhook to admin Slack. `checkDoorCode` distinguishes typo (`denied_unknown`) vs expired (`denied_retired`) for accurate ops reports. New `AdminDoorAccessPanel`: side-by-side active codes list (color-coded code tile, "OVERDUE Xd" badge when stale, Rotate/Retire buttons) + last-100 entries feed (auto-refresh 30s). Issue-code modal with customer search (reuses `searchCustomersForLabel` from iter-128), 30/60/90/180-day rotation chips, optional notes, generated-code reveal screen with copy-friendly large display. Wired into Operations nav.
58. **Customer onboarding video walkthrough** ✓ shipped iter-148 — three new schema models: `OnboardingVideo` (slug @unique, title, videoUrl, posterUrl, durationSec, sortIndex, isActive), `OnboardingVideoView` (per-user, monotonically increasing percentWatched via `max(existing, new)`, completedAt set first time ≥95%), `OnboardingTouch` (idempotency for the email sequence). Server actions: admin CRUD `listOnboardingVideos`/`upsertOnboardingVideo`/`deleteOnboardingVideo`, member `getMyOnboardingPlaylist`/`recordVideoView` (upserts max-pct, only audit-logs the FIRST completion event to avoid log spam), cron-driven `runOnboardingTouchSweep` for Day 0/2/5 welcome emails (idempotent via OnboardingTouch unique). New `/api/cron/onboarding-touch` Bearer-auth route (run daily). Admin panel with poster preview, viewer-count + completion-pct stats, full editor (slug auto-normalized, video URL validation, sort-index, active toggle). Member `<WelcomePanel>` with overall progress hero + 2-col playlist grid (poster + ▶ overlay + completed badge + per-video progress bar) + inline player modal with timeupdate-driven 25/50/75/100 threshold reporting + special "Mark as watched" button for YouTube/Vimeo iframe embeds (where progress can't be tracked). Wired into both admin nav (Customers) and member nav (Mailbox · Welcome tab).
59. **Photo OCR address parser** ✓ shipped iter-149 — `src/lib/aiAddressOcr.ts` calls Claude Vision (Haiku 4.5, 600 max tokens, 15s timeout) with a strict OCR-focused system prompt that asks for `{recipientName, recipientCo, suiteNumber, trackingNumber, carrier, senderName, confidence, notes}` JSON. Defensive parsing strips code-fences, validates carrier enum, normalizes suite (digits-only, strips STE/PMB/Suite#/etc) and tracking (whitespace stripped, uppercase) to match iter-141 conventions. Server action `parseEnvelopePhoto({imageUrl})` chains OCR → iter-139 smart routing on the extracted recipient name → returns combined result with `routedTopMatch` candidate. Audit-logs `intake.ocr_parsed` (with confidence + carrier + match score) or `intake.ocr_failed` (with reason). Wired into `AdminInboundScanPanel`: gradient "📷 Scan envelope (auto-fill)" button (uses native `capture="environment"` for tablet rear camera), in-flight loading state, surfaces a Parsed Fields card with confidence pill (high green / medium amber / low red), 6-field grid (Recipient/Suite/Tracking/Carrier/c-o/Sender), green "✨ Smart-matched X · suite #N · 92% match" line when iter-139 matched a customer, italic admin note line, "Apply to form" (auto-fills + auto-picks the matched customer) and "Discard" buttons. Image is reused as the package's exterior photo — no double-upload.
60. **Inventory shop / supplies management** ✓ shipped iter-150 — new `Supply` model (name, category 7-way `boxes|tape|labels|poly_mailers|envelopes|printer_ribbon|other`, unit `each|roll|box|case|ream`, onHand, reorderAt threshold, reorderQty suggested order size, vendor, vendorSku, costCents, isActive) + `SupplyMovement` log (kind 5-way `restock|sale|internal_use|loss|adjust`, signed delta, performedBy, notes). Status auto-derives: `out` ≤0, `low` ≤reorderAt, `ok` otherwise. Server actions `listSupplies`/`upsertSupply`/`deleteSupply`/`recordSupplyMovement` (atomic supply.update + movement.create + audit `supply.movement_<kind>` in single $transaction; restock = positive delta, sale/internal_use/loss = negative, adjust = signed-delta computed from new absolute on-hand). New `AdminSuppliesPanel`: 4 status filter chips + 7 category chips with live counts, sortable table (Item · On-hand × unit · Reorder threshold · Status pill · Edit), per-row drawer with movement-history list + "Record movement" form (5 kind chips, qty/setOnHand input, notes), full-fields editor modal with vendor/SKU/cost/active toggle. Wired into Operations nav.
61. **Email deliverability dashboard** ✓ shipped iter-151 — `getEmailDeliverabilityReport({windowDays?})` rolls up `EmailLog` over 7/30/90-day windows: per-status totals (sent/failed/bounced/queued), per-kind breakdown with failure %, daily volume gappy-fill array. Live SPF/DKIM/DMARC checks via Cloudflare DoH (`https://cloudflare-dns.com/dns-query`) with TXT character-string concatenation, status grading (no record = fail, missing all-mech / no DMARC / soft-fail = warn, strict / Resend include / DMARC quarantine|reject = ok), DKIM probes 8 common selectors. Audit-logs `email.deliverability_report_ran` with window + DNS statuses. Admin panel: window-selector chips (7/30/90d), 5 headline tiles (Total/Delivered/Failed/Bounced/Queued), color-coded overall-health banner (≥10% fail = red, ≥3% = amber, else green), DNS authentication card with per-record status pill + first-line preview, sortable per-kind table (Kind/Sent/Failed/Bounced/Failure% with red/amber/green tone/Last sent), green+red stacked daily sparkline with hover tooltip showing date + counts, recent failures+bounces list (last 25 with status pill, recipient, kind, subject, italic error message). Wired into System nav.
62. **Customer-of-the-month** ✓ shipped iter-152 — new `CustomerOfMonthAward` schema with `@@unique([year, month])` enforcing one winner per month at the DB level (race-safe). Server action `nominateCustomerOfMonth` runs `prisma.$transaction([award.create, audit.create])` then fires-and-forgets a celebratory email (`sendCustomerOfMonthEmail` — gold gradient hero + citation pull-quote + "View your badge" CTA) and a `customer.of_month_awarded` webhook. Friendly P2002 race-collision error + pre-create existence check. `listCotmAwards`/`deleteCotmAward`/`getMyCotmAwards` complete the surface (delete logs `customer.of_month_revoked`). Admin panel groups awards by year, gold-tinted cards with month chip + customer + citation blockquote + revoke. Nominate modal: month/year selectors, customer search reusing iter-128 `searchCustomersForLabel`, citation textarea (10–1000 chars). Member-side `<CustomerOfMonthBadge>` renders nothing for non-winners; for winners, gold-gradient hero at the TOP of the dashboard overview with month + citation + multi-win counter. Webhook event registered in iter-103 enum.
63. **Renewal-discount auto-offer** ✓ shipped iter-153 — new `RenewalDiscountOffer` schema (userId, code @unique, percentOff, expiresAt, sentAt, redeemedAt, redeemedById, reason). `runRenewalDiscountSweep()` finds members with `mailboxStatus="Active"` AND `planDueDate ≤ today+30d`, blocks anyone with an active offer OR a sent-within-90d offer, computes iter-140 health score for the rest, sends one-time discount (default 10% off, 21d valid) to everyone in `Watch` or `At Risk` buckets. `prisma.$transaction([offer.create, audit.create])` then fire-and-forget congrats email (`sendRenewalDiscountOfferEmail` — gradient hero with mono code + expiry date) + `renewal.discount_offer_sent` webhook. Code generator uses cuid-style 8-char alphabet (no I/O/0/1) prefixed `NOHO-XXXX-XXXX`. New `/api/cron/renewal-discount-sweep` Bearer-auth route. Member side: `getMyActiveRenewalOffer()` returns the in-flight offer (non-redeemed, non-expired) with `daysRemaining` countdown; `<RenewalDiscountCard>` renders nothing for non-targets, gradient blue hero with "🎁 N% off your renewal" + expiry days + Copy button (clipboard API + ✓ Copied flash) for winners; mounted on dashboard overview. `redeemRenewalDiscountOffer({code})` validates ownership/expiry/redemption then atomic mark-redeemed with audit. Admin panel: 3 status tiles (Active/Redeemed/Expired), "Run sweep now" button with result toast, full table with status pill + reason citation.
64. **Bureau live "open/closed" page** ✓ shipped iter-154 — new public route `/open` (under marketing layout, no auth) renders live status from `getLiveBureauStatus()`: combines iter-90 `isOpenNow()` (open/closing_soon/closed_today/closed_holiday + minutesUntilClose), admin-set `staffOnDuty` (new SiteConfig key), `awaitingPickupCount` from MailItem ("queue depth" signal), and the full week of hours. Server action audits `bureau.staff_on_duty_set`. Page features: animated pulse on the live dot when open, color-coded headline (green open / amber closing soon / red closed) with countdown subhead, "On duty" + "Pickups waiting" stat tiles, Directions/Phone/Dashboard CTAs, full week table with today highlighted in cream. New `<AdminStaffOnDutyCard>` mounts in the Operating Hours panel — admin types staff name, sees live preview "X is here for you", clears with one click, ✓ Saved flash. `dynamic = "force-dynamic"` so every refresh shows current minute. Mobile-friendly grid.
65. **Receipt printer support** ✓ shipped iter-155 — new 80mm narrow thermal-receipt route at `/admin/print/thermal/{kind}/{id}` (kind = `intake` | `pickup`). Auto-prints on load via `<AutoPrint />` client wrapper (250ms delay so layout settles). `@page size: 80mm auto; margin: 0` + `-webkit-print-color-adjust: exact` ensures Star Micronics / Epson narrow receipt printers get a clean single-strip output. Receipt sections: dashed-border brand header (NOHO MAILBOX + address + phone), receipt heading + timestamp + ref-id (last 6 of mailItemId), Customer block (name/suite/phone), Package block (from/recipient/carrier/tracking-mono/weight/dim), conditional storage-fee block on pickup-kind, conditional signature block when iter-137 signature exists, footer with QR to dashboard + brand quote + "thank you" copy. New `src/lib/print-station.ts` localStorage helpers + `<AdminPrintStationPicker>` widget on the Settings panel: per-device-only "Station name / Receipt printer / Label printer" inputs that admin uses as muscle-memory cues when the OS print dialog opens (browsers can't programmatically pick a printer). Hooked into `AdminInboundScanPanel` as a "🧾 80mm" button next to the existing "4×6 receipt" link. Bonus: fixed pre-existing tsc error in `square.ts` (`squarePaymentId: { not: null }` on a required field) — full project tsc clean.

---

**Tier 9 closes at iter-155 — all 10 items shipped (iters 145-155).**

## Tier 10 · Bureau growth (added at iter-155)

66. **Member referral leaderboard** ✓ shipped iter-156 — read-only rollup over the existing iter-29 `Referral` schema (no schema changes). Server actions: `getReferralLeaderboard({window: "month" | "all_time"})` returns top 10 with rank-collision-aware ranking (ties share a rank, next rank skips), customer name resolution batched via `findMany({id: in: …})`, sums by referrerId via `prisma.referral.groupBy`; `getMyReferralStanding()` returns my month/all-time rank+count + total $ earned + #1's count for "invite N more to take #1" copy; `getReferralLeaderboardAdmin()` returns both windows. Member-side `<ReferralLeaderboardCard>` mounted on dashboard overview right above the cross-sell upsells: monthly title with month name, "you're #4 with 3" personal callout, top 5 list with 🥇🥈🥉 medals + gold-tinted rows for top 3 + "🎁 Get my code" CTA, contextual zero-state copy ("be first to refer this month"), gap-callout ("invite N more to tie/take #1"). Admin panel side-by-side month + all-time boards with medal styling, total-credited and total-customers stats in headers, refresh button.
67. **Geo-radius marketing flyer** ✓ shipped iter-157 — server actions `getZipMarketIntel({zip})` (counts deliveries-to-ZIP + forwarding-address ZIP matches + 5 adjacent ZIPs sharing the first 3 digits + % of bureau activity) and `getTopZipsByActivity({limit})` for the "where to flyer next" suggestion list. ZIP normalization to 5 digits + validation. New `<AdminMarketingFlyerPanel>`: ZIP input form with Get-Intel button, 4 stat tiles (Deliveries to ZIP / Forwarding lines / % of all deliveries / Adjacent ZIPs), clickable adjacent + top-by-activity ZIP chips for one-click drill-down, Print button. Print-ready 8.5×11 Letter flyer (cqw container queries so the flyer scales nicely on screen and clamps to paper size in print): cream brand background, decorative gradient corners, big "Hello, {city} neighbors at {zip}" headline (auto-derives city from leading 3 digits), pitch paragraph, 3-tile benefit grid (Every carrier / Mail in your phone / Real address), pricing strip with three plan badges, big "$50/mo" anchor, signup URL + bureau address + phone, QR code linking to `?utm_source=flyer&utm_campaign=zip{zip}` for attribution. `@page Letter` + `flyer-print` visibility-only print CSS so the panel chrome hides on print.
68. **Plan upgrade nudge** ✓ shipped iter-158 — `getPlanNudgeContext()` reads existing tables only (no schema changes): only nudges Basic members with `mailItem` count ≥10 OR package count ≥3 in last 90 days; recommends `Premium` when packages ≥10 OR mail ≥30 (heavy), else `Business` (moderate); generates human-readable reason copy ("You've handled 7 packages + 12 mail items in 90 days — Business unlocks unlimited packages…"). Member `<PlanUpgradeNudgeCard>` renders ONLY when shouldNudge: cream gradient hero with radial blue glow, "You've outgrown Basic" eyebrow + bold "Time for {plan}? $X/mo" headline, reason paragraph, 4-feature checklist (different list per Premium vs Business), 1-click "Upgrade to {plan}" CTA wired to existing iter-116 `requestPlanUpgrade`, "Compare plans →" secondary link, "Not now" dismiss with 7-day localStorage grace cookie + auto-dismiss after successful upgrade. Mounted on dashboard overview between RenewalDiscount and ReferralLeaderboard cards.
69. **Bureau weekly newsletter** ✓ shipped iter-159 — new `NewsletterIssue` schema (`weekKey @unique` ISO-week format `2026-W19`, weekStart/weekEnd, subject, editorial text, bodyHtml snapshot, recipientCount + failedCount, sentAt/sentById). `compileWeeklyDigest()` rolls up the previous Mon→Sun: total mail handled, packages, pickups (from `mail.status.picked_up` audit log), forwards (from `mail.status.forwarded`), top weekly referrer (iter-156 join), current customer-of-the-month (iter-152), upcoming 30-day holidays (iter-90). HTML email template with gradient header + 4 stat tiles + cotm gold card + editorial pull-quote + holidays callout + dashboard CTA. `sendWeeklyNewsletter` (admin-gated) + `sendWeeklyNewsletterAsSystem` (cron-callable wrapper) + `previewWeeklyNewsletter` + `listNewsletterIssues`. Idempotent on weekKey (force-resend overrides). New `/api/cron/weekly-newsletter` Bearer-auth route runs Mondays. Admin panel: 4 stat tiles, editorial textarea (1200-char cap) with live `<iframe sandbox>` preview re-renderable on demand, force-resend checkbox, "📨 Send now" with confirm + result toast, past-issues history (weekKey · subject · ✓N · ✕N · date). Audit-logged as `newsletter.weekly_sent`.
70. **Notary booking calendar** ✓ shipped iter-160 — no schema changes (reuses iter-29 `NotaryBooking`). Server actions: `getNotaryAvailability({dayCount?})` derives 30-min slots from iter-90 operating hours (handles holidays + closed days + close-time buffer of 30min), pulls all Pending+Confirmed bookings in window in one query and marks taken slots, computes `earliestSlot` for "first open" hint; `createNotaryBookingForSlot({date,time,type,notes?})` atomic via `prisma.$transaction([conflict-check, booking.create, audit.create])` with race-safe double-book guard returning friendly "slot just booked — pick another" error; `cancelMyNotaryBooking({id, reason?})` ownership-gated with `notary.booking_cancelled` audit; `listMyNotaryBookings()` member-side. New `<NotaryBookingCalendar>` mounted inside existing NotaryPanel: 14-day horizontal date strip with weekday labels + open-slot count + closed-day badges, 30-min slot grid (3-6 cols responsive) with cream chip styling + line-through for taken + selected blue, expanding confirm form with 7 doc-type chips + Other free-text option, "📒 Book this slot" CTA with conflict error on race, upcoming-bookings list below with status pill + Cancel button.
71. **PWA install prompts** ✓ shipped iter-161 — `public/manifest.json` upgraded with `id`, `start_url=/dashboard`, `scope=/`, `orientation=portrait`, brand cream background + NOHO blue theme color, `categories: [business, productivity, lifestyle]`, 3 icon entries (192/512 PNG + maskable SVG), 3 manifest `shortcuts` (Mail/Packages/Notary deep-links). New `<PwaInstallPrompt>` client component: counts distinct visit days in localStorage (capped at 30 keys), captures `beforeinstallprompt` and defers it, shows our own gradient blue card at the bottom of the screen ONLY after 3+ distinct days AND not already installed AND not 60-day-dismissed. iOS Safari fallback (no `beforeinstallprompt` API): UA-detects iPhone Safari and shows Share→Add-to-Home-Screen instructions. `appinstalled` event auto-dismisses + persists. Mounted at both `/dashboard/layout.tsx` and `/admin/layout.tsx` so members and counter staff each get their own deferred prompt.
72. **Customer photos export** ✓ shipped iter-162 — added `jszip` dep. New `GET /api/photos/export` route (Node runtime, force-dynamic, member-gated): pulls all `MailItem`s for the user, streams an `application/zip` Content-Disposition attachment containing intake photos (`exteriorImageUrl`), mail scans (`scanImageUrl`), iter-135 multi-photo gallery extras (parsed from `extraPhotosJson`), and iter-137 pickup signatures (already self-contained SVG documents). Per-file 25MB safety cap, 10s fetch timeout, defensive image-fetch helper that returns null on failure. Filenames include `<YYYY-MM-DD>-<refId>-<sender-slug>` with descriptive suffixes (front/scan/extra-{label}/signature) so the customer's photo roll is browsable, not a hash dump. README.txt at the ZIP root explains the folder layout. Audit-logged as `member.photos_exported` with added/skipped/totalBytes/itemsScanned. Member dashboard `<PhotosPanel>` got a "⬇ Download all (.zip)" button anchored to the right of the filter chips.
73. **Bulk SMS campaigns** ✓ shipped iter-163 — new `BulkSmsCampaign` schema (label, body, audienceJson, recipientCount, succeededCount, failedCount, notSentCount, estSegments, sentById, sentAt) + `SmsLog.bulkBatchId` foreign key so per-recipient logs can be drilled-into per campaign. Reusable `src/lib/sms-segments.ts` `computeSegments()` (GSM-7 vs UCS-2, single 160 / multi 153, single 70 / multi 67) so client can show live segment + cost preview without server roundtrip. New `src/app/actions/bulkSms.ts`: `previewBulkSmsAudience({audience, body, marketingOptIn})` returns total / optedIn / withGoodPhone / reachable + 8-row sample + 8-row unreachable sample with reason; `sendBulkSms({label, audience, body, marketingOptIn, testPhone, dryRun})` test-path skips audience entirely + tags `[TEST]` body; real path filters non-admin users with phone, applies in-memory `notifPrefs.marketing.sms` opt-in (or relaxed `mailArrived.sms` when toggle off), normalizes E.164, sends via existing `sendSms()`, stamps each `SmsLog` row with `bulkBatchId = campaign.id`, atomic `BulkSmsCampaign` create + final `update` with totals + roll-up `sms.bulk_campaign_sent` audit; `listBulkSmsCampaigns(limit)` + `getBulkSmsAudienceOptions()`. New `<AdminBulkSmsPanel>` mounted under Messages → "Bulk SMS" sub-tool: 4-tile audience snapshot (active / with phone / SMS opted-in / reachable), audience picker (All / By plan / Expired / Explicit phones), strict-marketing-opt-in toggle, 3 quick-template chips (Holiday hours / Storage clearout / Open house), label input, body textarea with live segment + cost preview ($0.0079/seg estimate), test-phone send with 1-click, debounced audience preview, iPhone-bubble live preview with `{{firstName}}` substitution, reachable + unreachable sample lists, "Send to N" CTA double-gated by confirm() + typed-SEND prompt for >100 recipients, past-campaigns history list with ✓/✕/⊘ counts + segments + date.
74. **Inventory pricing tiers** ✓ shipped iter-164 — extends iter-150 supplies with per-Supply price tiers + a 30-day margin report. New `SupplyPriceTier` schema (id, supplyId, label, salePriceCents, isDefault, position, notes, createdAt/updatedAt; @@unique([supplyId,label]) so no two "Retail" on one supply; @@index for [supplyId,position] + [supplyId,isDefault]). New columns on `SupplyMovement`: `unitPriceCents`/`unitCostCents`/`tierLabel`/`customerId` so sales capture the price snapshot at sale time → reports stay stable when the catalog price drifts later. New `@@index([kind, performedAt])` so the margin window query stays bounded. Helpers `computeMarkupPct()` + `computeMarginPct()` + `DEFAULT_TIER_PRESETS` (Retail 100% / Member 60% / Wholesale 25%) live in `src/lib/supplies-config.ts` (shared between server action + client UI per "use server" constraint). Server actions: `listSupplyPriceTiers({supplyId})`, `upsertSupplyPriceTier({id?, supplyId, label, salePriceCents, isDefault, position, notes})` (auto-demotes other defaults via updateMany before write, audit `supply.tier_created/updated`), `deleteSupplyPriceTier({id})` (audit `supply.tier_deleted`), `getInventoryMarginReport({windowDays?})` (last 7/30/90/365 days; sums revenue from `unitPriceCents × |delta|`, COGS from `unitCostCents × |delta|` with current `costCents` fallback for pre-iter rows; returns blendedMarginPct + per-supply rows sorted by profit + topTierLabel from in-window sales). `recordSupplyMovement` extended: when kind="sale" auto-resolves `tierId` (or default tier) → captures `unitPriceCents`/`unitCostCents`/`tierLabel`/`customerId` snapshot in the SupplyMovement row + threads them into the audit metadata. `listSupplies()` now returns `defaultTier` + `tierCount` per row in one Prisma include + `_count.priceTiers`. `listSupplyMovements()` surfaces `unitPriceCents`/`unitCostCents`/`tierLabel`. AdminSuppliesPanel additions: 4-tile margin widget at top (Units sold / Revenue / Profit / Blended margin %) with 7/30/90/365-day toggles + 6 top-profit shortcut chips (clickable → open drawer); new Price·Margin column on the inventory table showing default tier price + label + colored margin pill (≥50% green / ≥20% amber / else red) + "+N" badge for additional tiers; drawer's sale form gains a Pricing-Tier dropdown (auto-prefills from default ★) + Unit Price override input + live "Revenue × COGS = Profit · margin %" preview before save; sale rows in the movement history list now show the captured tier badge + "$X × N = $TOTAL · profit $X" line; new SupplyPriceTiersBlock inside the drawer with 3 preset buttons (greyed out when already added), inline add/edit row with live markup+margin preview, "Make default" button per tier (atomic re-default via the demote+upsert path), delete button (past sales keep their captured price snapshot). All margin math respects null cost (returns null markup vs nonsensical Infinity).
75. **API tokens for power-user customers** ✓ shipped iter-166 — new `ApiToken` schema (id, userId FK with cascade-delete, name, `tokenPrefix @unique` for cheap lookup, `tokenHash @unique` SHA-256-hex of the full plaintext, `scopesJson` defaulting `[]`, lastUsedAt + lastUsedIp, expiresAt nullable, revokedAt + revokedReason, indexes on [userId, createdAt] + [revokedAt]) + new `ApiTokenUsage` (per-request log: tokenId FK cascade, endpoint, method, status, ip, userAgent, durationMs, createdAt; indexes on [tokenId, createdAt] + [createdAt]) + User.apiTokens relation. Token format `noho_<8charPrefix><32charBody>` (40 hex chars total of 20 random bytes). New `src/lib/apiTokens.ts` (lives outside "use server" so route handlers can import it): `generateToken()` (crypto.randomBytes), `hashToken(plaintext)` SHA-256-hex, `parseScopes(json)` / `serializeScopes(scopes)` round-trip with allowlist, `extractBearerToken(req)` accepts `Authorization: Bearer …` OR `X-Api-Token`, `verifyApiToken(req, requiredScope)` is the single source of truth (lookup-by-prefix → constant-time `timingSafeEqual` of full hash → revokedAt/expiresAt/scope gates → returns `{userId, tokenId, scopes}` on ok or `{status, error}` on fail), `logApiUsage()` fire-and-forget tx that writes the usage row + bumps the token's `lastUsedAt`/`lastUsedIp`, `clientIp(req)` x-forwarded-for parser. 4 scopes: `profile:read`, `mail:read`, `packages:read`, `billing:read`. Server actions in `src/app/actions/apiTokens.ts`: `listMyApiTokens()` returns metadata only with a 24h request count groupBy in one query (never the plaintext); `createApiToken({name, scopes, expiresInDays?})` enforces 10-active-tokens-per-account cap, generates the token, atomically creates row + audit `member.api_token_created`, returns the plaintext ONCE; `revokeMyApiToken({id, reason?})` ownership-gated atomic update + audit `member.api_token_revoked`; `getMyApiTokenUsage({tokenId, limit})` for the drilldown. Reusable route helper `src/app/api/v1/_handler.ts` `withApiToken(scope, handler)` wraps each route with bearer-auth + scope gate + try/catch + fire-and-forget usage logging + `x-noho-api: v1` response header. Three v1 endpoints shipped: `GET /api/v1/me` (profile snapshot — name/email/phone/suite/plan/status/planDueDate/memberSince), `GET /api/v1/mail` (paginated `?limit=1-200&offset=&type=&status=&since=ISO8601` returning id/type/status/from/date/label/weightOz/dimensions/carrier/trackingNumber/recipientName/priority/photo URLs/pickedUpAt/createdAt + `total`/`nextOffset` cursor), `GET /api/v1/packages` (sugar over mail with `type=Package` + default `notIn ["Picked Up","Forwarded"]` filter + `?includeDelivered=1`). Member-side `<ApiTokensCard>` mounted at the bottom of SettingsPanel: empty-state "Generate first token" CTA, list of tokens (active/expired/revoked colored pill + scope chips + last-used relative time + IP + 24h req count + created/expires badges + "View activity" expandable usage table + "Revoke" with confirm), 4-scope checkbox grid + Never/30d/90d/1y expiration picker + token-name input, post-create modal that shows the plaintext ONCE in a select-all monospace block + 1-click "Copy" button + a ready-to-paste `curl -H "Authorization: Bearer …"` example with terminal-styled green-on-black formatting + a "🚨 it will not be shown again" warning. tsc clean exit=0.

---

### iter-165 — Hotfix (label printer print preview)

User report: "the review for print in label printer does not work you cannot print". Root cause: the off-screen wrapper `<div style="position:absolute; left:-9999px">` made itself the **positioned ancestor** for the inner `.label-print-area`. The print CSS's `position: absolute; left: 0` therefore resolved against that wrapper — putting the inner label at `-9999 + 0 = -9999px`, well outside the page box → blank preview. Fixed in `AdminLabelPrinterPanel.tsx` by switching the inner to `position: fixed` (always anchors to the page box in print, ignoring ancestor offsets) AND replacing the `left: -9999` wrapper trick with a clean `display: none` on screen + `display: block` in print (no positioning needed). `tsc --noEmit` exit=0. Lesson logged: never put `position: absolute` inside an off-screen ancestor — `fixed` is the right tool for "anchor to the page" behavior.

---

## Tier 11 — Continuing the loop (added at iter-167+)

All 45 Tier 7-10 items shipped iter-112 → iter-166. Tier 11 keeps the loop going with the next remarkable features:

76. **Inbound webhooks for member integrations** ✓ shipped iter-167 — sister to iter-104 admin webhooks but per-MEMBER + privacy-scoped to userId (the dispatcher hard-filters on owner so no cross-tenant broadcast is even possible). New `MemberWebhook` schema (id, userId FK cascade, label, url HTTPS-only, `secret` plaintext-stored 48-hex `whsec_*` shown ONCE, events JSON array, active toggle, `failureCount` consecutive-fail counter that auto-disables at 50, lastFiredAt + lastStatus, revokedAt nullable; index on [userId, active] + [revokedAt]) + `MemberWebhookDelivery` per-attempt log mirroring the iter-134 retry+dead-letter shape (attempt int, nextRetryAt, deadLettered, lastTriedAt, indexes on [webhookId, sentAt] + [nextRetryAt] + [deadLettered, sentAt]) + `User.memberWebhooks` relation. Lib `src/lib/memberWebhooks.ts`: 10 member-scoped events (mail.arrived/scanned/picked_up/forwarded, package.arrived/picked_up, id.expiring, plan.expiring_soon, appointment.upcoming, test.ping — deliberately smaller than admin set since members never see aggregates), `generateWebhookSecret()` 192-bit, `signPayload(secret, body)` returns `sha256=<hex>`, `fireMemberWebhooks(userId, event, payload)` is the single side-effect call (loads ONLY active+non-revoked webhooks for THAT user, filters to subscribed events, signs each delivery with its own secret, POSTs to user URL with `redirect: "manual"` to defeat URL-bait, `AbortSignal.timeout(8000)`, headers `X-NOHO-Event` + `X-NOHO-Signature` + `User-Agent`, atomic delivery+counter update inside tx, auto-disable + audit `member.webhook_auto_disabled` after 50 consecutive fails, trim to 200 most recent successful deliveries per webhook), `retryMemberDelivery(deliveryId)` + `drainMemberWebhookRetries()` exact mirror of iter-134 retry shape with 6-attempt backoff [60s/5m/30m/2h/12h]. New `/api/cron/retry-member-webhooks` Bearer-CRON_SECRET route. Server actions `src/app/actions/memberWebhooks.ts`: `listMyMemberWebhooks()` with 24h delivery count via groupBy, `createMyMemberWebhook({label, url, events})` validates HTTPS-only + rejects localhost/.local, 5-active cap, returns secret ONCE + audit `member.webhook_created`, `revokeMyMemberWebhook({id, reason?})`, `setMyMemberWebhookActive({id, active})` pause/resume with auto-resetting failureCount on resume, `testMyMemberWebhook({id})` ensures test.ping in subscription set then fires a manual ping, `getMyMemberWebhookDeliveries({webhookId, limit})` ownership-gated drilldown with httpStatus/error/duration/attempt/deadLettered. Wired into existing iter-83 `logScannedInbound` (fires both `package.arrived` AND generic `mail.arrived` for OWNER) and `updateMailStatus` pickup path (fires `package.picked_up` for packages + `mail.picked_up` for everything). Member-side `<MemberWebhooksCard>` mounted under `<ApiTokensCard>` in SettingsPanel: empty-state "Register first webhook", per-row label + URL + event chips + colored status pill (DELIVERED green / READY blue / PAUSED amber / FAILING red / REVOKED grey) + 24h count + consecutive-fail count + last-fired relative + 4 actions (🧪 Test / Activity drawer with delivery log / Pause-Resume / Revoke), create form with HTTPS validation + 10-event checkbox grid, post-create modal showing the secret ONCE in a select-all monospace block + 1-click Copy + a ready-to-paste Node.js HMAC verification snippet in terminal-styled green-on-black + headers/payload-shape doc + "🚨 it will not be shown again" warning. Auto-test path re-loads activity 1.5s after firing so the test ping appears in the drawer. tsc clean exit=0.
77. **Mail-item public sender thank-you** ✓ shipped iter-168 — extends iter-93's public share page (`/p/[id]?t=…`) with a sender-side note form so the person who shipped a package can drop a short message that lands in the recipient's dashboard. New `SenderThankYou` schema (id, mailItemId FK cascade, senderName/senderEmail nullable, message 1-500, ip + userAgent diagnostic-only, hidden Boolean for spam control, indexes on [mailItemId, createdAt] + [createdAt]) + MailItem.senderThankYous relation. Server actions in `src/app/actions/senderThankYou.ts`: `submitSenderThankYou({mailItemId, shareToken, message, senderName?, senderEmail?})` is the public entry-point — validates against the MailItem's `publicShareToken`, validates email shape, captures ip/userAgent from `headers()`, two-tier rate limit (5 notes per package per IP per hour + 30 global per IP per hour), creates the row, fires `createNotification` to the recipient with type=general + title `"📬 Note from <senderName>"` + 100-char body preview + dashboard deeplink, fires `mail.scanned` member webhook for the OWNER with `kind: "sender_thank_you"` detail (so power-user members can pipe sender notes into their tooling), writes audit `mail.sender_note_submitted` with `actorRole: "PUBLIC"`. `listMySenderNotes({limit?, includeHidden?})` joins the MailItem to surface the front photo + sender for each note. `hideSenderNote({id, hidden})` ownership-gated. `getSenderNoteCountForPublic({mailItemId, shareToken})` returns count for social proof on the share page. New `<SenderThankYouForm>` client component embedded into `/p/[id]/page.tsx`: cream-bg textarea with 500-char counter + 5 emoji-prefixed suggested-message chips ("🎁 Hope you love it!" / "💕 Thinking of you" / "🎂 Happy birthday!" / "🙏 Thank you for everything" / "✨ A little something for you"), optional name + email side-by-side inputs, large blue "📬 Send to <firstName>" CTA, success state replaces form with a green border + "💌 Note sent to <firstName>" confirmation. Form auto-suppresses when MailItem is `Returned` or `Discarded` (no point) but stays visible for `Picked Up`/`Forwarded` so the sender can still leave a thank-you after delivery. Recipient's first name is derived from `recipientName` so the form copy reads "Hey Sami…" naturally. Member-side `<SenderNotesCard>` mounted on the dashboard overview (between PlanUpgradeNudge and the hero band): renders nothing for empty mailboxes, otherwise shows a 3-up grid (1/2/3 cols responsive) with 12×12 package thumbnail + sender name + 100-char preview + relative time, click to expand inline (full message + Hide/Unhide toggle + clickable mailto: if email captured), "Show all" toggle reveals up to 100 notes including hidden ones. tsc clean exit=0.
78. **Member-of-the-month spotlight on marketing site** ✓ shipped iter-169 — extends iter-152 CustomerOfMonthAward with member-controlled homepage spotlight opt-in flow. New `CotmMarketingSlot` schema (id, awardId @unique 1:1 with the award row, userId, status enum `pending`/`published`/`retracted`/`rejected`, businessName ≤80, quote ≤200, photoUrl + websiteUrl HTTPS-validated, publishedAt + publishedById, retractedAt, rejectedAt + rejectionReason, indexes on [status, publishedAt] + [userId]) + CustomerOfMonthAward.marketingSlot 1:1 relation + User.cotmMarketingSlots relation. Server actions in `src/app/actions/cotmMarketingSlot.ts`: `submitCotmSpotlight({awardId, businessName, quote, photoUrl?, websiteUrl?})` ownership-gated, validates HTTPS + length, supports re-submission flow (clears rejection state, flips back to pending), atomic upsert + audit `cotm.spotlight_submitted`; `retractMyCotmSpotlight({id})` audit `cotm.spotlight_retracted`; `getMyCotmSlots()` member view; `getMyAwardIdForMonth({year, month})` resolver helper so the opt-in form can find the awardId without exposing all IDs in the listing; admin-side `listAdminCotmSlots({status?})`, `publishCotmSpotlight({id})` audit `cotm.spotlight_published`, `rejectCotmSpotlight({id, reason})` audit `cotm.spotlight_rejected` + reason persisted for member to read; `getCurrentMarketingSpotlight()` returns the single most-recent published slot for the homepage (try/catch swallows errors so a schema mismatch never breaks the marketing page). Member-side `<CotmSpotlightOptInCard>` mounted on dashboard overview directly under `<CustomerOfMonthBadge>` (renders nothing for non-winners): full opt-in form with business name + website URL + photo URL + quote textarea (200-char counter); 4-state status view (PENDING amber / PUBLISHED green with "Live since <date>" + Take Down / RETRACTED grey / REJECTED red with rejection reason + Edit-and-Resubmit), photo thumbnail + quote preview + clickable website. New `<MarketingSpotlightSection>` server component slotted between TESTIMONIALS and VISIT-US on `/(marketing)/page.tsx`: cream gradient panel with "★ Customer of the Month · <month>" pill, large business-name headline, member-name eyebrow, framed photo (or 🌟 fallback gradient tile) with gold border + glow, italic quote with gold left-border, inline blue "Visit <business> →" CTA when website provided, footer line "Want to be featured? Sign in & opt in." with dashboard link. New admin panel `<AdminCotmSpotlightPanel>` at `/admin?tab=cotmspotlight`: 5 status filter chips (defaults to Pending), per-row card with 80×80 photo, business name + 4-state status badge + member info + suite # + award month + italic quote with gold border + clickable website + admin-citation italic + rejection-reason red text, ✓ Publish + Reject buttons with confirm() and prompt() flows. Wired into nav. tsc clean exit=0.
79. **Recurring scheduled forwarding** ✓ shipped iter-170 — promotes iter-129's manual one-shot forwarding to a true cron-driven recurring sweep with vacation pause + per-batch audit + member-visible history + member webhooks integration. Schema additions: `ScheduledForwarding.enabled` Boolean (soft pause vs hard `active=false` cancel) + `pauseUntil` YYYY-MM-DD vacation mode + `notes` member free-text + `lastBatchSize` Int + `lastBatchAt` DateTime + new `@@index([enabled, active, nextRunDate])`. New `ScheduledForwardingBatch` schema (id, forwardingId FK cascade, userId denormalized, addressLabel + addressBody snapshots so address edits don't rewrite history, itemCount, itemIdsJson full list, notes copied at run time, source enum cron/manual/test, shippedAt; indexes on [userId, shippedAt] + [forwardingId, shippedAt]). Reusable `src/lib/scheduledForwarding.ts` (lives outside "use server"): `computeNextRunDate(frequency, fromIsoDate?)` pure date math, `todayYmd()`, `isDueNow(row)` predicate the cron + UI both use, `ALL_FREQUENCIES` + `FREQUENCY_LABELS` enums. New email `sendScheduledForwardingBatchEmail({...})` in lib/email.ts: branded layout + h1 + cream address-snapshot block with white-space-pre-line + member-note pull-quote + next-batch-date sentence + dashboard CTA + "pause from dashboard" footer. New cron route `/api/cron/scheduled-forwarding-sweep` (Bearer-CRON_SECRET). Server actions in new `src/app/actions/scheduledForwardingBatch.ts`: `runScheduledForwardingSweep()` no-auth system entry walks every active+enabled row with `nextRunDate <= today`, skips paused, calls per-row processor, returns `{processed, shipped, empty, skippedPaused, failed, totalItems}` summary; private `processForwardingRowInternal(forwardingId, source)` is the atomic per-row processor (resolves address with default-to-first fallback, queries every eligible MailItem in `Received`/`Scanned`/`Awaiting Pickup`/`Held`, ATOMIC `prisma.$transaction(async tx)` flips items to `Forwarded` + creates ScheduledForwardingBatch row + rolls forward nextRunDate + writes audit `mail.scheduled_forward_batch` with full itemIds list and address snapshot, fires `sendScheduledForwardingBatchEmail` + `fireMemberWebhooks(userId, "mail.forwarded", …)` + admin Slack/Discord via `fireWebhooks("mail.picked_up", …)` — all fire-and-forget so a side-effect never blocks the commit; rolls forward + writes `mail.scheduled_forward_empty` audit even when no items to keep cadence; rolls forward + writes `mail.scheduled_forward_skipped_no_address` when member deleted all forwarding addresses); `setRecurringForwarding({frequency, addressId, notes})` upsert with restored-from-pause cleanup + audit `mail.scheduled_forward_set`; `pauseMyForwarding({untilDate})` validates future date + audit `mail.scheduled_forward_paused`; `resumeMyForwarding()` clears pauseUntil; `runMyForwardingNow()` member-fired manual batch with source=manual; `listMyForwardingBatches({limit})` history; `getMyForwardingStatus()` returns full state including `isDueNow` derived flag + addresses join. UI: replaced the iter-129 minimal `<ScheduledForwardingCard>` in SettingsPanel with full lifecycle card — 2-col snapshot tile (Next batch + Last batch with item count) + amber "⏸ Paused — resumes <date>" badge when paused + italic note pull-quote + 5-button action bar (Edit / 📬 Run now with confirm / Pause-for-vacation `<details>` with date input / Resume / Cancel) + collapsible batch history with item count + destination + date + source tag (cron/manual/test) for each shipment. Edit form gets new notes input. Defense-in-depth: save action falls back to iter-129 `setScheduledForwarding` if the new server action errors during the schema rollout window. tsc clean exit=0.
80. **Lockbox keypad live status board** ✓ shipped iter-171 — new `Lockbox` schema (cuid, label @unique, location, serial, isActive, state enum closed/open/ajar/fault, openedAt + openedByUserId + openedByEntryId + openedReason + expectedCloseBy + lastClosedAt, notes, indexes on [state, isActive] + [openedAt]) + `LockboxEvent` per-state-change log (lockboxId cascade, kind opened/closed/fault/manual_override, userId, doorEntryId, reason, durationSec on closes, expectedCloseBy snapshot, indexes on [lockboxId, recordedAt] + [kind, recordedAt] + [userId, recordedAt]). Reusable `src/lib/lockbox-config.ts` (outside "use server"): `LockboxState` union, `LOCKBOX_STATES` 4-tuple with emojis, `DEFAULT_OPEN_WINDOW_SEC = 300`, `LockboxOpenReason` enum + labels, `fmtDuration(sec)` shared by board + drawer. Server actions in `src/app/actions/lockbox.ts`: `getLockboxBoard()` returns `{tiles, openCount, overdueCount, recentEvents}` with derived `openSeconds` + `isOverdue` flags + batched user-name resolution in one query; `upsertLockbox`/`deleteLockbox` CRUD with audit `lockbox.created/updated/deleted`; `markLockboxOpened({id, userId?, doorEntryId?, reason?, expectedOpenSeconds?})` atomic state-change tx that flips state + writes LockboxEvent + audit `lockbox.opened` + fires admin webhook (`door.code_issued` reused) + fires member webhook `package.picked_up` for the unlocking member if known; `markLockboxClosed({id, reason?})` symmetric with `durationSec` snapshot + audit `lockbox.closed`; `setLockboxFault({id, isFault, reason?})` with audit `lockbox.fault_marked/cleared`; `closeOverdueLockboxesSweep()` cron-callable system entry walks state="open" + expectedCloseBy<now, atomic close + writes LockboxEvent kind=manual_override reason="auto_closed_overdue" + audit `lockbox.auto_closed_overdue` + admin webhook ⚠️ alert. New `/api/cron/close-overdue-lockboxes` Bearer-CRON_SECRET drain route runs every 5 min. Admin `<AdminLockboxBoardPanel>` mounted at `/admin?tab=lockboxboard`: tablet-friendly responsive grid (2/3/4/5 cols), 4 stat tiles (total / open now / overdue / + Add CTA), tile colors green-closed / blue-open-in-window / RED-overdue / amber-ajar / red-fault / grey-inactive, **live 1s tick** updates open-duration counters without DB hits, **5s polling** refreshes board state, side activity feed shows last 20 events with kind+user+duration+time, click any tile opens inspector drawer with state hero showing live timer, member name, reason, expected-close-by + open/close/fault action buttons (single button "🔒 Mark closed" when open, two buttons "🔓 Open admin" / "🛠 Service open" when closed), edit form has label+location+serial+notes+isActive checkbox + delete with confirm. `aria-live="polite"` sr-only counter announces open/overdue counts to screen readers. Wired into Operations nav as "Lockbox Board". tsc clean exit=0.
81. **Mailing list export to Mailchimp / ConvertKit** ✓ shipped iter-172 — multi-provider ESP audience sync with opt-in honoring + persistent audit. New `EspAudienceSync` schema (id, provider enum mailchimp/convertkit/buttondown/csv, label, audienceId, apiKey + apiServer plaintext-stored, isActive, marketingOptInOnly default true, doubleOptIn, lastSyncAt + lastSyncCount + lastSyncFailed + lastSyncError + lastSyncBy snapshots, indexes on isActive) + `EspSyncRun` per-run log (audienceId FK cascade, source enum manual/scheduled/test, total/added/skipped/failed counts, error string, durationMs, startedAt/finishedAt, startedBy; index on [audienceId, startedAt]). New shared types in `src/lib/esp/types.ts` (outside "use server"): `EspProvider` union, `PROVIDER_LABELS` + `PROVIDER_DESCRIPTIONS`, `EspContact` interface (email/name/firstName/lastName/meta), `EspProviderModule` `pushContacts(config, contacts)` interface, `ProviderSyncResult` `{added, skipped, failed, error?}`. Three provider modules: `lib/esp/mailchimp.ts` PUTs to `/lists/{audienceId}/members/{md5(email)}` with HTTP Basic auth — treats HTTP 400 with title `Member Exists` as skip not failure; `lib/esp/convertkit.ts` POSTs to `/v3/forms/{formId}/subscribe` with API key in body; `lib/esp/buttondown.ts` POSTs to `/v1/subscribers` with `Authorization: Token` header — treats 400/409 as duplicates/skip. All providers use `AbortSignal.timeout(8000)`. Server actions in `src/app/actions/espAudienceSync.ts`: `listEspAudienceSyncs()` masks apiKey to `hasApiKey: boolean` for the panel; `upsertEspAudienceSync({...})` validates per-provider required fields (CSV needs nothing, Buttondown skips audienceId, Mailchimp/CK both required), preserves existing apiKey when admin leaves the field blank on edit, audit `esp.audience_created/updated`; `deleteEspAudienceSync` audit `esp.audience_deleted`; `previewEspAudience({marketingOptInOnly})` walks all non-admin users in JS, validates email shape, applies the `notifPrefs.marketing.email` predicate when strict, returns `{total, withEmail, eligible, optedOut, noEmail, sample[8]}` so admin sees opt-in rate at a glance; `runEspAudienceSync({id, source?})` 60-second min-between-runs throttle, creates EspSyncRun row first, resolves eligible contacts, hands off to provider module (or builds a CSV string for csv provider with proper escaping for embedded commas/quotes/newlines), finalizes run + stamps audience.lastSync* + writes audit `esp.audience_synced`; CSV path returns `{csvDownloadName, csvBody}` so the panel can browser-download via `URL.createObjectURL(new Blob(...))`. `listEspSyncRuns({audienceId, limit})` history. Admin `<AdminEspSyncPanel>` mounted at `/admin?tab=espsync`: 4-tile preview snapshot (total / with email / opt-in / opt-in rate %), per-row audience card with provider badge + audienceId + apiServer + opt-in chips + last-sync stats (added green / failed red / error red / never-synced italic) + 4-action footer (📥 Download CSV or ↻ Sync now / History toggle / Edit / Delete), expandable history shows last 12 runs (OK/ERR badge + source + +N added + ~N skipped + ✕N failed + ms + relative time + truncated error tooltip), editor modal with 2-col provider picker grid + provider-aware required fields (CSV is keyless, Buttondown skips audienceId, Mailchimp shows DC suffix with auto-detect note), API key input shows "leave blank to keep current" placeholder when editing, 3 boolean checkboxes (strict marketing opt-in / double opt-in / active). Wired into Communications nav. tsc clean exit=0.
82. **Carrier-pickup scheduling** ✓ shipped iter-173 — admin schedules USPS/UPS/FedEx/DHL pickups for outbound batches with full state machine + auto no-show alerts. New `CarrierPickup` schema (carrier enum, pickupDate YYYY-MM-DD, pickupWindowOpen/Close HH:MM 24-hour, packageCount + totalWeightOz estimate, locationNote + instructions free-form, confirmationNumber from carrier portal, status enum draft/scheduled/completed/missed/cancelled, scheduledById + scheduledAt + completedAt + completedActualCount + cancelledAt + cancelReason + missedAt + driverName + notes, indexes on [status, pickupDate] + [carrier, pickupDate] + [pickupDate]). Reusable `src/lib/carrier-pickup-config.ts` (outside "use server"): `CarrierPickupCarrier` + `CarrierPickupStatus` unions, `CARRIER_PICKUP_CARRIERS` 5-tuple with emojis + carrier portal URLs (USPS/UPS/FedEx/DHL each), `CARRIER_PICKUP_STATUSES` 5-tuple with bg/fg colors, `pickupStatusStyle()` + `carrierMeta()` helpers. Server actions in `src/app/actions/carrierPickup.ts`: `listCarrierPickups({status?, carrier?, fromDate?, toDate?})` filtered query; `upsertCarrierPickup({...})` validates YYYY-MM-DD + HH:MM + future date + window-close-after-open, auto-flips to "scheduled" status when confirmationNumber provided on create, audit `carrier_pickup.created/updated`; state-transition actions `markPickupScheduled({id, confirmationNumber})` + `markPickupCompleted({id, actualPackageCount?, driverName?})` + `cancelCarrierPickup({id, reason?})` + `deleteCarrierPickup({id})` each with atomic update + per-action audit (`carrier_pickup.scheduled/completed/cancelled/deleted`); `sweepMissedCarrierPickups()` no-auth cron entry walks status="scheduled" + pickupDate≤today, computes window-close-plus-60min-grace timestamp, atomic update to "missed" + audit `carrier_pickup.auto_missed` + admin webhook ⚠️ alert per row with package count + confirmation # in detail; `getCarrierPickupCounts()` returns header tiles (todayScheduled / todayPending drafts / missedLast7d / nextPickup row). New `/api/cron/sweep-missed-pickups` Bearer-CRON_SECRET drain route runs every 15min. Admin `<AdminCarrierPickupPanel>` mounted at `/admin?tab=carrierpickup`: 4 stat tiles header (Today scheduled / Today drafts / Missed-7d / Next-pickup quick-launch tile that opens editor on click), 2-row filter chips (status × carrier), date-grouped row list with "Today · Mon May 8" / "Tomorrow · …" / "Yesterday · …" headers, per-row card with carrier emoji + window + status pill + package count + weight + location + confirmation # mono + driver instructions italic + status-specific footer line (✓ completed timestamp + driver, ⚠️ auto-marked-missed timestamp, cancellation reason), 6-action button stack (🗓 Schedule with prompt for confirmation # / ✓ Mark done with prompt for actual count + driver / Cancel with reason prompt / Edit / "Open portal ↗" deeplink to carrier's actual portal page / × Delete with confirm), full editor modal with date+time+time picker grid + package-count + weight-lb (auto-converts to oz) + location + driver instructions + confirmation # field that toggles draft/scheduled status. Wired into Operations nav. tsc clean exit=0.
83. **Notary witness check-in** ✓ shipped iter-174 — extends iter-160 NotaryBooking with full counter-side check-in workflow + chain-of-custody scan attachment + member webhook on completion. Schema additions: `NotaryBooking.notes` (member-supplied at booking) + `checkedInAt` + `checkedInById` + `idPresented` enum (DL/Passport/State ID/Military/Other) + `idPresentedNumber` (last-4 OK) + `signedDocumentUrl` (Vercel Blob HTTPS) + `completedAt` + `completedById` + `witnessName` + `noShowAt` + `noShowReason` + `adminNotes` + new `@@index([status, date])` + `@@index([date, time])`. Server actions in `src/app/actions/notaryCheckIn.ts`: `listNotaryCheckIns({date?, status?})` filtered list with user join (suite + email + name); `checkInNotaryBooking({id, idPresented, idPresentedNumber?, witnessName?, adminNotes?})` ID-type validated against allowlist, atomic flip to status="Checked In" + clear noShow fields + audit `notary.checked_in`; `attachSignedDocument({id, signedDocumentUrl})` URL-validated, can be called multiple times to replace, audit `notary.signed_doc_attached`; `completeNotaryBooking({id, signedDocumentUrl?, adminNotes?})` requires checkedInAt sentinel, atomic flip to status="Completed" + freezes the row + audit `notary.completed` + fires `appointment.upcoming` member webhook with `kind: "notary_completed"` detail (so power-users can pipe receipts into their own tooling); `noShowNotaryBooking({id, reason?})` clears check-in fields + flips status="No Show" + audit `notary.no_show`; `getNotaryCheckInCounts()` returns today tile counts (booked / checked-in / completed / pending). Admin `<AdminNotaryCheckInPanel>` mounted as sub-tool of /admin?tab=notary (also reachable directly at `/admin?tab=notarycheckin`): 4 stat tiles header (today's booked/checked-in/completed/pending), date picker + Today reset button + 7-status filter chips, time-grouped row list with 12h-formatted headers (10:00 AM, 10:30 AM, …), per-row card with customer name + status pill + suite # + type + member-supplied notes + check-in line ("🕐 Checked in 10:23 AM · ID: DL (••1234) · 👤 Witness: Karim") + completed line ("✓ Completed 10:54 AM · view scan ↗") + no-show line + admin-notes italic + 3-action stack (🕐 Check-in modal pre-filled with previous values, 📎 Attach/Replace doc with URL prompt, ✓ Complete modal with Blob URL field + final notes, No-show with reason prompt). Check-in modal: 5-button ID-type chip row + last-4 input + witness name input + admin notes textarea. Complete modal: signed-document-URL input + final-notes textarea (carries forward existing values for both). Wired into Operations nav (sub-tool of Notary). tsc clean exit=0.
84. **Member loyalty tiers** ✓ shipped iter-175 — 4-tier loyalty system computed from existing tables (no new input data) + nightly cron promotion sweep + member dashboard card + promotion email + member webhook. Schema additions: `User.loyaltyTier` String? + `loyaltyTierAt` DateTime? + relation to `LoyaltyTierAssignment`. New `LoyaltyTierAssignment` schema (per-transition history: tier, previousTier, reasonsJson snapshotting which qualifying axes were met at the time, scoreSnapshot, triggeredBy enum system/admin/manual; indexes on [userId, createdAt] + [tier, createdAt]). Reusable `src/lib/loyalty-config.ts` (outside "use server"): 4-tier `LoyaltyTier` union (Bronze/Silver/Gold/Platinum), `TIER_META` per-tier metadata (emoji + bg/fg/accent colors + tagline + benefits[] list), `TIER_RENEWAL_DISCOUNT` per-tier % (0/5/10/15) auto-applied at renewal, `TIER_REQUIREMENTS` qualifying axes per tier (Bronze=active mailbox; Silver=≥6mo + no overdue; Gold=≥12mo + paid plan + no overdue + 90d engagement; Platinum=≥24mo + Premium/Business plan + no overdue + iter-140 health score ≥80), `tierIndex()` + `nextTierAbove()` helpers. Server actions in `src/app/actions/loyaltyTiers.ts`: `computeMemberLoyaltyTier(userId)` pure compute (queries tenure days from User.createdAt, overdueCount via Invoice with try/catch, engagement90d via MailItem, healthScore via existing `getCustomerHealthScore`), per-tier axis evaluation with met/detail per axis, returns `{tier, axesByTier, scoreSnapshot, nextTier, remainingForNext}`; private `persistTierIfChanged()` does atomic User update + LoyaltyTierAssignment append + audit `loyalty.promoted`/`loyalty.demoted` + fires `sendEmail({kind: "loyalty_promoted:<tier>"})` ONLY on promotion (never on demote — no "you've been downgraded" notice) + member webhook `plan.expiring_soon` with `kind: "loyalty_promoted"` detail; `getMyLoyaltyTier()` member-facing; `recomputeUserLoyaltyTier({userId})` admin-side single-user re-run; `runLoyaltyTierSweep()` no-auth cron entry walks all non-admin users + returns `{scanned, promoted, demoted, unchanged, failed}` summary; `getLoyaltyTierDistribution()` admin distribution + recent-12 promotions list. Promotion email: tier-color gradient header with 48px emoji + tagline + dashboard CTA + benefits bullet list + auto-applied discount % callout. New `/api/cron/loyalty-tier-sweep` Bearer-CRON_SECRET nightly route. Member-side `<LoyaltyTierCard>` mounted on dashboard overview between CotmSpotlight and the hero band: tier-color radial-gradient hero with 64×64 emoji medallion + "Loyalty tier · …" eyebrow + bold tier name + italic tagline + auto-applied-discount pill, 2-col split below: "What you get today" benefits checklist with tier-accent ✓ marks, "Path to <next>" remaining-axes checklist with circle markers + (detail) inline showing exact gap (e.g. "Member ≥ 6 months (123d / 180d)"), or "You're at the top" terminal state for Platinum members. tsc clean exit=0.
85. **In-person dropoff barcode generator** ✓ shipped iter-176 — public kiosk page where members generate a 4×6 printable barcode receipt BEFORE arriving with a package, then admin scans the barcode at the front desk and intake auto-fills + saves in one tap. New `DropoffBarcode` schema (id, code @unique 12-char alphanumeric Crockford-base32 minus 0/O/1/I to avoid scan ambiguity, userId optional for opportunistic resolution, suiteNumber + expectedSender + expectedTracking + expectedCarrier + photoUrl + notes captured from kiosk form, expiresAt 14 days, claimedAt + claimedById + claimedMailItemId, indexes on [code] + [userId, createdAt] + [claimedAt] + [expiresAt]). Reusable `src/lib/dropoff-barcode.ts` (outside "use server"): `generateDropoffCode()` rejection-sampled crypto random over 32-char alphabet, `formatDropoffCode(code)` → "XXXX-XXXX-XXXX" pretty form for printing, `normalizeDropoffCode(raw)` strips dashes + lowercase → canonical form for DB lookup, `DROPOFF_CARRIERS` 6-tuple, `DROPOFF_EXPIRY_DAYS = 14` constant. Server actions in `src/app/actions/dropoffBarcode.ts`: `generateDropoffBarcode({suiteNumber, expectedSender, expectedTracking?, expectedCarrier?, notes?})` is the public no-auth entry — opportunistically captures session userId when present + IP-rate-limited via AuditLog scan (8 codes/hr/IP), retries on (very rare) collision, audit `dropoff.barcode_generated`; `getDropoffBarcodeByCode({code})` public lookup for the receipt page; `lookupDropoffBarcode({code})` admin scan returns row (errors on already-claimed or expired); `claimDropoffBarcode({code, mailItemId})` ownership-gated freeze + audit `dropoff.barcode_claimed`; `listRecentDropoffBarcodes({status, limit})` admin pending/claimed/expired filtered list; `listMyPendingDropoffBarcodes()` member-side. Public kiosk pages: `/dropoff` server-component frame + `<KioskForm>` client component (suite + sender required, carrier 6-chip picker, optional tracking, optional 500-char note for staff, large blue "🖨 Generate barcode" CTA with monospace suite input); `/dropoff/[code]` printable receipt server-component renders Code 128 barcode inline via existing iter-125 `generateCode128()` lib, 4×6 layout with brand header + suite # callout + sender block + monospace tracking line + member-note italic block + centered barcode-flex section + "Show at the front desk" footer + expires-on date, `@page { size: 4in 6in }` print CSS with `position: fixed` (uses iter-165 fix pattern!) + `display: none` outside print + `visibility: hidden` on body+`.receipt`-only-visible trick, claimed/expired no-print banners with green/red coloring, `<PrintButton>` extracted client component (window.print() + new code link). Admin `<AdminDropoffBarcodePanel>` mounted at `/admin?tab=dropoffscan`: 2-col layout with autofocus scan input on left + recent codes sidebar on right, scan input strips dashes via normalizeDropoffCode, on lookup shows blue 2-border auto-fill preview card with suite + sender + carrier + tracking + member note, "✓ Save intake + claim" button calls existing iter-83 `logScannedInbound` (member gets the full mail-arrived treatment: email + push + member webhook) → on success calls `claimDropoffBarcode` to freeze row, fallback tracking format `DROPOFF-<code>` when member didn't supply one. Recent sidebar: 4-status filter (pending/claimed/expired/all) + click-to-prefill scan input, status badges + suite + sender preview + tracking. Wired into Operations nav as "Dropoff Scan". tsc clean exit=0.



---

## Tier 12 — Continuing the loop (added at iter-177+)

All Tier 11 items shipped iter-167 → iter-176. Tier 12 keeps the loop going with the next remarkable features:

86. **Stripe ACH renewal payments** ✓ shipped iter-177 — adds Stripe ACH/bank-debit alongside iter-89's Square card-only path. Materially lower fees on big-ticket Business renewals (~0.8% capped at $5 vs Square's 2.9% + $0.30 → savings of $35+ on a $500 renewal). New `StripeAchMandate` schema (cuid, userId cascade, stripeCustomerId `cus_*`, stripePaymentMethod `pm_*`, stripeMandateId `mandate_*` populated after first successful charge for re-use, bankName + bankLast4 captured at setup, status enum pending/active/revoked/failed, setupCompletedAt + revokedAt + revokedReason + notes, indexes on [userId, status] + [status, createdAt]) + `StripeAchPayment` per-charge log (mandateId FK cascade, denormalized userId, amountCents, description, stripePaymentIntentId @unique, stripeChargeId, status enum processing/succeeded/failed/cancelled, failureCode + failureMessage, initiatedById admin or null for cron, settledAt). Stripe wrapper `src/lib/stripeAch.ts` (outside "use server"): no SDK dep — direct REST with `AbortSignal.timeout(10s)`, `STRIPE_SECRET_KEY` env var with **graceful no-op contract** (`isStripeConfigured()` predicate + every fn returns `{ok: false, reason: "not_configured"}` so app builds without Stripe set up). Server actions in `src/app/actions/stripeAch.ts`: `setupAchForMyUser()` returns Stripe SetupIntent client_secret; `recordAchMandate({stripeCustomerId, stripePaymentMethodId, bankName?, bankLast4?})` auto-revokes prior active mandate (one active per member), audit `stripe.ach_mandate_created`; `revokeAchMandate({id, reason?})` ownership-gated soft-delete + audit; `chargeAchForRenewal({mandateId, amountCents, description})` admin-fired creates StripeAchPayment row with status="processing" THEN calls Stripe → updates with PI id + status (ACH stays "processing" 4-5 business days, webhook will flip later) + audit `stripe.ach_charge_processing/succeeded/failed`; `getMyAchSummary()` returns `{active, recentPayments, configured}`. Member `<AchPaymentCard>` mounted under `<MemberWebhooksCard>` in SettingsPanel: renders nothing when Stripe isn't configured AND no historical activity, shows amber "ACH isn't configured" warning when keys missing, active mandate displayed as 12×12 blue 🏦 tile + bank name + ····last4 + green ACTIVE pill + Remove button, no-mandate state shows "🏦 Connect bank account" CTA, recent-payments table with status pills (✓ SETTLED green / ⏳ PROCESSING blue / ✕ FAILED red / CANCELLED grey). tsc clean exit=0.
87. **Photo album auto-organize by sender** ✓ shipped iter-178 — extends iter-115 PhotosPanel + iter-162 photo album with intelligent sender bucketing so members can browse "all my Amazon packages this year" instead of scrolling a flat firehose. New `src/lib/sender-normalize.ts` (outside "use server"): 28-rule needle-match table maps wildly-varying sender stamps ("AMAZON.COM SERVICES LLC", "Amazon Logistics", "amzn") into one canonical "Amazon" bucket with 📦 emoji + #FF9900 brand accent. Pre-defined buckets cover the long tail: Amazon Fresh / UPS / FedEx / USPS / DHL / Temu / Shein / Wayfair / IKEA / Walmart / Target / Costco / Best Buy / Apple / Microsoft / Chewy / Sephora / Ulta / Nike / Lululemon / Etsy / IRS / CA Tax Board / Bank of America / Chase / Wells Fargo / Capital One / Amex — each with brand-color accent + emoji. `bucketForSender(raw)` returns `{key, bucket, emoji, accent}` — first needle match wins (so "amazon fresh" maps to Fresh before bare "amazon"); unknown senders fall back to title-cased original with strip of trailing Inc/LLC/Corp suffixes (so "Marie Khiari" still groups her own letters). Server action `getMyPhotoAlbumGroupedBySender({filter?, windowDays?})` walks the same MailItem set as iter-115 (1000-row safety cap), passes each `from` through `bucketForSender`, accumulates per-bucket photo counts + first/last seen + 6-photo sample, returns groups sorted by item count desc (most-active sender first). PhotosPanel additions: new view-mode toggle "📷 Grid" / "📁 By sender" alongside the existing All/Packages/Mail filter chips, sender-grouped view renders 1/2/3-col responsive grid of cards (each: 44×44 brand-tinted emoji tile with bucket accent border + bucket name + "N shipments · M photos" + "since <month year>" + 3-up sample-photo strip), click any card to open the bucket detail view (back button + bucket header + 4-col photo grid sharing the same lightbox handler as flat view + footer note steering to grid view when itemCount > 6 sample). View toggle resets openGroupKey on switch. Status counter swaps between "X of Y photos" (flat) and "N senders · M photos" (grouped). tsc clean exit=0.
88. **Customer self-serve plan downgrade** ✓ shipped iter-179 — counterpart to iter-116's prorate-on-upgrade. Downgrades DON'T prorate (no refund — sidesteps a customer-service nightmare); the lower price kicks in at next renewal. New `PlanDowngradeRequest` schema (cuid, userId cascade, fromPlan + toPlan snapshots at request time, effectiveAt YYYY-MM-DD = current planDueDate snapshot, optional reason ≤500, status enum pending/approved/applied/cancelled/denied with full timestamp set + cancelledBy member/admin attribution + deniedReason member-visible + approvedById, indexes on [userId, createdAt] + [status, effectiveAt]) + User.planDowngradeRequests relation. Server actions in `src/app/actions/planDowngrade.ts`: private `checkDowngradeEligibility(userId)` evaluates 7 axes (active mailbox / paid plan / has lower tier to move to / no overdue invoices / no active vacation hold / no open StorageFeeDispute by `filedById` / no pending downgrade) returning `{ok, reasons[], currentPlan, currentMonthlyCents, currentDueDate, options[], pendingRequestId}` so blocked members see exactly what to resolve; `getMyPlanDowngradeOptions()` member-side wrapper; `requestPlanDowngrade({toPlan, reason?})` validates target is strictly cheaper than current, atomic create + audit `plan.downgrade_requested` + admin Slack/Discord ping (churn-risk events deserve human eyeballs) + member confirmation email with side-by-side savings table; `cancelMyPlanDowngrade({id})` ownership-gated soft-cancel + audit; `listMyPlanDowngrades()` member history; `listAdminPlanDowngrades({status?})` admin queue; `approvePlanDowngrade({id})` admin approval + audit `plan.downgrade_approved`; `denyPlanDowngrade({id, reason})` member-visible rejection + audit; `applyDuePlanDowngradesSweep()` no-auth cron entry walks status="approved" + effectiveAt≤today, atomic User.plan flip + status="applied" + audit `plan.downgrade_applied` + sends "you're now on X" green-gradient applied-email + fires `plan.expiring_soon` member webhook with `kind: "plan_downgrade_applied"` detail. New `/api/cron/apply-due-plan-downgrades` Bearer-CRON_SECRET daily route. Member-side `<PlanDowngradeCard>` mounted under `<PlanUpgradeCard>` in SettingsPanel: renders nothing for non-paid or already-Basic members, 3-state UI — eligible state shows 1/2-col plan picker cards (each: plan name + $X/mo + green "Save $Y/mo" pill) + optional reason textarea + "Schedule <plan> downgrade" CTA + effective-date footer; blocked state shows amber checklist of unmet eligibility axes with per-axis detail (e.g. "No overdue invoices (2 overdue)"); in-flight state shows status pill + from→to + effective date + member-supplied reason + "Cancel downgrade" button; past-requests history list with status pills (PENDING amber / APPROVED blue / APPLIED green / CANCELLED grey / DENIED red + denied-reason inline). tsc clean exit=0.
89. **iCal feed for member appointments** ✓ shipped iter-180 — subscribable iCal feed at `/api/v1/calendar.ics` token-gated via iter-166 API tokens with new `calendar:read` scope. Calendar apps (Apple Calendar, Google Calendar, Outlook) can subscribe + auto-refresh hourly so notary appointments + pickups + plan renewal + ID expirations all appear in the member's normal calendar without leaving NOHO. Extended `src/lib/apiTokens.ts` API_SCOPES with new `calendar:read` entry. New pure-TS `src/lib/ical.ts` (outside "use server") RFC 5545 builder: `IcsEvent` + `IcsCalendar` types, `buildIcs(cal)` emits VCALENDAR/VEVENT with VERSION/PRODID/CALSCALE/METHOD/X-WR-CALNAME/X-WR-CALDESC headers + REFRESH-INTERVAL hint (1h), per-event UID/DTSTAMP/DTSTART/DTEND/SUMMARY/DESCRIPTION/LOCATION/URL/CATEGORIES with proper RFC 5545 §3.3.11 text escaping (`\\` `\,` `\;` `\n`) + 72-octet line folding (RFC 5545 §3.1) + UTC `YYYYMMDDTHHMMSSZ` timestamps + DATE-form `YYYYMMDD` for all-day events. New `src/app/api/v1/calendar.ics/route.ts`: dual-auth (`Authorization: Bearer …` for tooling OR `?token=noho_…` query param for calendar subscriptions which can't send custom headers) via `verifyForCalendar()` wrapper that synthesizes the auth header from the query param when present, surfaces 4 event types — iter-29/160 NotaryBooking (30-min events with check-in status in description), iter-101 PickupAppointment (durationMin minutes from `scheduledAt`, packageCount in title, guestName + notes in description, try/catch swallows schema-shape mismatch), `User.planDueDate` as all-day "💳 Mailbox renewal due", `User.idPrimaryExpDate`/`idSecondaryExpDate` as all-day "🪪 ID expires" reminders with the actual ID type from `idPrimaryType` — every event gets bureau location + dashboard deeplink URL + category. Uses iter-166 `logApiUsage` for per-fetch audit. 401 fallback returns helpful plain-text body so calendar apps surface a debuggable error. Member-side `<ApiTokensCard>` SecretReveal modal extended: when the freshly-created token includes `calendar:read`, an extra blue calendar-subscribe block renders with paste-instructions for Apple/Google/Outlook + the full URL `https://nohomailbox.org/api/v1/calendar.ics?token=…` in select-all monospace + 1-click "Copy URL" button. tsc clean exit=0.
90. **Drop-in mailbox tour booking** ✓ shipped iter-181 — public marketing-site form lets prospects request a 15-min walkthrough of the bureau before signing up. New `MailboxTour` schema (cuid, name + email + phone, requestedDate YYYY-MM-DD, requestedTime HH:MM, partySize 1-10, free-text reason ≤500, status enum Pending/Confirmed/Completed/No Show/Cancelled, source enum web/phone/walkin, ip + userAgent diagnostic-only, full timestamp set + confirmedById + completedById + cancelReason + adminNotes, **becameMember + becameUserId for conversion attribution**, indexes on [status, requestedDate] + [requestedDate, requestedTime] + [email]). Server actions in `src/app/actions/mailboxTour.ts`: `requestMailboxTour({...})` is the public no-auth entry — strict email regex, future-date enforcement, IP-rate-limit via AuditLog scan (4/hr/IP), atomic create + audit `tour.requested` + auto-acknowledgment email + admin Slack/Discord ping with prospect details; `confirmMailboxTour({id})` admin-side flips status + writes audit `tour.confirmed` + fires green-gradient confirmation email with date/time/location + Apple Maps deeplink CTA; `markTourCompleted({id, becameMember?, becameUserId?, adminNotes?})` audit `tour.completed` or `tour.completed_converted` (separate action key for conversion analytics); `markTourNoShow({id, reason?})` and `cancelMailboxTour({id, reason?})` round out the state machine; `getMailboxTourCounts()` returns header tiles `{pending, confirmedToday, completedThisMonth, conversionRate30d}` with the conversion rate auto-computed from completed-30d × becameMember-30d. Public `/tour` page: brand-cream hero + "Drop in for 15 minutes" headline + `<TourForm>` client component (name + email + phone + date+time picker + party size 1-10 + free-text "what brings you in" + 14px blue CTA), success state replaces form with green-bordered "🎟 We got your request!" confirmation. Two transactional emails: `tour_acknowledged` cream-bg with side-by-side details table + member's reason as italic pull-quote + "we'll confirm in a few hours" copy; `tour_confirmed` green-gradient hero + same details + Apple Maps "Open in Maps" CTA. Admin `<AdminMailboxToursPanel>` mounted at `/admin?tab=mailboxtours`: 4 stat tiles header (Pending / Confirmed today / Completed this month / **Conversion rate 30d** with green/amber/red threshold coloring), 6-status filter chips (Pending default), per-row card with name + status pill + ✓ CONVERTED green badge when applicable + 🗓 date·time + party size + clickable mailto: + clickable tel: + reason italic + admin notes + "requested Xd ago" timestamp + 4-action stack (✓ Confirm / ✓ Mark done with conversion confirm() prompt + notes prompt / No-show with reason / Cancel with reason). Wired into Money & Comms nav as "Tour Bookings". tsc clean exit=0.
91. **Per-mailbox sticky pin notes** ✓ shipped iter-182 — distinct from iter-120 PinnedNotes (per-customer): pins are keyed on **suite #** so they surface on every iter-83 intake involving that suite, regardless of which member admin is processing. Counter staff never miss "hold rent check for K. on Tues" or "verify ID before release". New `SuitePinNote` schema (id, suiteNumber, body ≤500, color enum amber/red/blue/green for visual urgency, expiresAt nullable for time-bounded notes, dismissedAt + dismissedById, createdById, indexes on [suiteNumber, dismissedAt] + [expiresAt]). Server actions in `src/app/actions/suitePinNotes.ts`: `getActivePinsForSuite(suiteNumber)` is the hot-path no-auth read used by iter-83 intake to surface pins on completion (active = not dismissed AND not expired); `listSuitePinNotes({suiteNumber?, activeOnly?})` admin filtered query with batched user-name resolution; `upsertSuitePinNote({id?, suiteNumber, body, color?, expiresAt?})` validates color allowlist + parses YYYY-MM-DD or ISO expiresAt + audit `suite_pin.created/updated`; `dismissSuitePinNote({id})` soft-dismiss with audit; `deleteSuitePinNote({id})` hard delete with audit (history kept in audit log); `getSuitePinCounts()` returns `{activePins, activeSuites, expiringSoon}` for the header. Wired into `logScannedInbound` (iter-83) — after intake commits, fetches active pins for the user's suite + returns them in the response shape `{success, mailItemId, suitePins[]}` so the scan UI can display them inline as a yellow alert. Admin `<AdminSuitePinsPanel>` mounted at `/admin?tab=suitepins`: 3 stat tiles header (active pins / suites pinned / expiring 7d with red threshold), suite # filter input + "Active only" checkbox, **sticky-note styled card grid** (1/2/3-col responsive) with -0.25deg rotation + drop shadow + 2-color border for tactile look, each card shows 📌 #suite headline + body + creator/date footer + 3-action row (Edit / Dismiss / × Delete) with status badges (DISMISSED grey / EXPIRED grey + 0.55 opacity), full editor modal with suite # + 500-char body textarea + 4-button urgency picker (🟡 Standard / 🔴 Urgent / 🔵 Info / 🟢 Resolved) + optional expires date. Wired into Operations nav as "Suite Pins". tsc clean exit=0.
92. **Multi-language member dashboard** ✓ shipped iter-183 — extends iter-110 cookie-only LanguageSwitcher with **server-side persistence** so the choice survives device changes (phone vs laptop) + sign-out cycles. New `User.locale` String? column. Updated `src/app/actions/locale.ts`: `getServerLocale()` now prefers User.locale (when authed) over cookie before falling through to Accept-Language → "en" — anonymous marketing-site visitors still use cookie+browser-language. New `setMyLocale(locale)` server action: cookie write + DB update + audit `member.locale_changed` (only when previousLocale ≠ newLocale, prevents audit-spam from re-saves). Falls back gracefully to cookie-only when not authed. Extended `src/lib/i18n/dictionary.ts` with new keys: `i18n.dashboard_language`, `i18n.choose_language_help` (long-form copy translated AR/FR/EN), `i18n.synced_across_devices`, `i18n.cookie_only`. New member `<LocaleSettingCard>` mounted under `<TimeZoneCard>` in SettingsPanel: 3-card responsive grid (English / Français / العربية) with flag emoji + native label + "EN/FR/AR · LTR/RTL" subtitle, active-state blue border, click immediately calls `setLocale` (instant UI flip via existing iter-110 `LocaleProvider` which auto-flips `document.documentElement.dir`) + `setMyLocale` (DB persist + audit), "✓ Synced across all your devices" footer. Arabic selection automatically RTL-flips the entire dashboard via existing iter-110 RTL plumbing. tsc clean exit=0.
93. **Carrier label rate shopping** ✓ shipped iter-184 — admin types destination zip + weight → side-by-side USPS / UPS / FedEx / DHL rate comparison with cheapest + fastest highlighted, persistent quote-snapshot audit. New `RateQuote` schema (originZip default 91601, destZip 5-digit, weightOz, optional dimensions + declaredValueCents, ratesJson snapshot, selectedCarrier/Service/TotalCents nullable, optional mailItemId link, createdById). Reusable `src/lib/rate-shop.ts` heuristic engine: `getRateQuotes()` returns 6 quotes (USPS Ground Advantage + USPS Priority + UPS Ground + FedEx Home + FedEx 2Day + DHL Express) with zone-by-first-digit-of-zip lookup × weight-ramp + per-zone-step bumps + insurance fee model ($1.50 base + $1/$100 over $100). Numbers calibrated to USPS Priority + UPS Ground retail circa 2025 — surfaces right cheapest carrier without burning live API credits. Server actions in `src/app/actions/rateShop.ts`: `quoteShippingRates()` validates 5-digit zip + positive weight + 70lb max, optionally persists with audit `rate_shop.quote_created`; `selectRateQuote()` matches snapshot + writes selectedCarrier + audit `rate_shop.carrier_selected` for carrier-mix reporting. Admin `<AdminRateShopPanel>` at `/admin?tab=rateshop`: 4-input form + Persist checkbox + "↻ Get rates" CTA, post-quote 1/2/3-col responsive grid of rate cards (carrier emoji + brand-color + service + ETA + big $X.XX, green border 💰 CHEAPEST / blue border ⚡ FASTEST badges), click to lock-in selection. Recent quotes strip below. Wired into Operations nav as "Rate Shop". tsc clean exit=0.
94. **Admin push notifications via Pushover/ntfy** ✓ shipped iter-185 — extends iter-104 admin webhook system with two new push-to-phone formats so the bureau owner gets a phone push the moment something needs attention (overdue lockbox, no-show carrier pickup, missed rate-shop quote). No schema change — adds to existing `WebhookEndpoint.format` enum. Refactored `lib/webhooks.ts`: replaced `formatBody()` with `buildRequest(format, url, event, payload)` returning `{url, headers, body, signableBody, persistableBody}` so each provider can have its own URL transform + content-type + body shape — Slack/Discord/generic stay JSON-POST; **Pushover** parses admin's pre-baked `?token=…&user=…` query params out of the URL → POSTs `application/x-www-form-urlencoded` to `https://api.pushover.net/1/messages.json` with `token + user + title="NOHO Mailbox · <event>" + message + url + url_title`; **ntfy** POSTs raw text body to whatever ntfy URL admin gave with `Title: NOHO Mailbox · <event>` + `Tags: mailbox,<emoji>` + `Click: <payload.url>` headers. Updated `deliver()` to use the new helper + persist the format-appropriate `persistableBody` (form-string for Pushover, plain text for ntfy) so `retryDelivery()` can replay it without re-rendering. Updated `retryDelivery()` to pick the matching Content-Type header per format + skip HMAC signing for ntfy (signature-less by spec). Server-action `webhooks.ts`: extended `WebhookRow.format` + `UpsertInput.format` + `VALID_FORMATS` set with `pushover` + `ntfy`. Admin `<AdminWebhooksPanel>` updates: 5-button format picker (Slack/Discord/generic/pushover/ntfy), `FormatPill` adds blue Pushover + green ntfy color tokens, URL auto-detection grew two new patterns (`api.pushover.net` or `?token=…` → pushover; `ntfy.sh` → ntfy), under-the-picker inline help text explains how to format the URL for each new provider with monospace example. tsc clean exit=0.
95. **Member-side billing dispute thread** ✓ shipped iter-186 — extends iter-86 StorageFeeDispute with member ↔ admin chat so both sides negotiate before resolution. New `DisputeMessage` schema (cuid, disputeId FK cascade, authorId, authorRole MEMBER/ADMIN, body 1-2000, **per-side read receipts** readByMemberAt + readByAdminAt for "N unread" badges) + StorageFeeDispute.messages relation. Server actions in `src/app/actions/disputeMessages.ts`: `getMyDisputeThread()` member-side ownership-gated with batched author-name resolution + `unreadForViewer` count; `getAdminDisputeThread()` admin equivalent; `postDisputeMessage()` auto-detects role from session, blocks closed-dispute posts, atomic create with self-side readAt + audit `dispute.member_replied`/`dispute.admin_replied` + cross-role notifications (member→admin fires admin Slack via `storage.dispute_filed`; admin→member fires `dispute_admin_reply` email + `test.ping` member webhook); `markDisputeRead()` viewer-side bulk-mark; `listAdminDisputeQueue()` with `unreadFromMember` count + `lastMessageAtIso` for sorting hot threads. Member `<DisputeThreadCard>` mounted on dashboard overview: renders nothing without open dispute, otherwise fee headline + "N new" red badge + member's reason italic, multi-dispute filter chips, **iMessage-style chat** with right-aligned blue member bubbles + left-aligned white admin bubbles + author label + timestamp + reply textarea. Admin `<AdminStorageDisputesPanel>` extended with "💬 Thread" button (always visible — works on closed disputes for read-only audit) opening `<ThreadModal>` with same iMessage-style chat (admin bubbles right blue) + composer that disappears for closed disputes. New `dispute_admin_reply` transactional email: bureau-blue gradient header + member greeting + admin reply rendered in italic blockquote + dashboard CTA + thread ID footer. tsc clean exit=0.

---

## Tier 13 — Continuing the loop (added at iter-187+)

All Tier 12 items shipped iter-177 → iter-186. Tier 13 keeps the loop going with the next remarkable features:

96. **Public proof-of-mailing certificate** ✓ shipped iter-187 — printable, publicly-verifiable chain-of-custody artifact members show in court / disputes / HR / IRS. New `MailingCertificate` schema (cuid, mailItemId, **certificateNumber @unique** human-readable `NMC-YYYY-NNNNNNNN`, **verifyToken @unique** 16-char Crockford-base32 minus 0/O/1/I, immutable snapshot fields recipientName/Suite/senderName/itemType/itemDate/trackingNumber/carrier/weightOz/exteriorImageUrl/notes so cert stays correct even if MailItem is later edited or deleted, issuedById, revokedAt + revokedReason for compliance recall). Reusable `src/lib/mailing-certificate.ts`: `generateCertificateNumber()` returns NMC-year-random8, `generateVerifyToken()` rejection-sampled crypto-random, `formatVerifyToken()` → "XXXX-XXXX-XXXX-XXXX" pretty form, `normalizeVerifyToken()` strips dashes. Server actions in `src/app/actions/mailingCertificate.ts`: `issueCertificateForMyMail()` member-side ownership-gated; `issueCertificateForMailItem()` admin equivalent; private `issueImpl()` retry-on-collision (5 attempts) + atomic create + audit `mailing_certificate.issued`; `getMailingCertificateByToken()` public no-auth verify lookup; `listMyMailingCertificates()` member history; `revokeMailingCertificate({id, reason})` admin invalidation + audit. Public `/cert/[token]` server-component verify page: 8.5×11" printable layout with bureau letterhead + Certificate № + formal "the undersigned bureau hereby certifies…" statement + 2-col details grid + custodian's note callout + photographic-evidence image when available + bottom Verify-Online section with pretty token + Code 128 barcode (via iter-125) + clickable URL + bureau-blue circular sigil + "custody system v1" attestation. Revoked certs render giant red "REVOKED" diagonal watermark + top warning banner. Print CSS uses iter-165 `position: fixed` pattern. `<PrintButton>` extracted client component. Member `<MailingCertificatesCard>` mounted in SettingsPanel: empty-state "+ Issue certificate" CTA, full state shows per-row cert № mono + sender + type + date + revoked badge + verifyToken path + "Open ↗" link, inline issuance form with mail item ID input + optional context note. tsc clean exit=0.
97. **Smart re-shelving suggestions** ✓ shipped iter-188 — daily cron + admin panel surface actionable nudges for keeping the mailbox grid right-sized. New `ReShelvingSuggestion` schema (cuid, kind enum dormant_suite/overflow_member/size_mismatch/vacant_box_for_waitlist, severity enum low/medium/high, suiteNumber + userId optional targets, reasonsJson snapshot, suggestedAction one-line plain-English, snoozedUntil + dismissedAt + actedAt state-machine, **`@@unique([kind, suiteNumber, userId])`** for cron idempotency). Server actions in `src/app/actions/reShelving.ts`: `listReshelfSuggestions()` admin filtered query with batched user resolution + true severity sort; `snoozeReshelfSuggestion()` + `dismissReshelfSuggestion()` + `markReshelfActed({id, note})` records what admin actually did + auto-dismisses + audit; `runReshelfSweep()` cron-callable walks all non-admin members, computes 30/90/180-day mail counts, applies 3 conservative heuristics: **dormant_suite** (paid plan + zero mail in 180d → reach out, downgrade or release), **overflow_member** (Basic + ≥12 items in 30d → upsell to Business), **size_mismatch** (Premium/Business + <3 items in 90d → suggest downgrade). Private `upsertSuggestion()` honors prior dismissals (won't re-create within 30d of dismissal — respects admin's choice). New `/api/cron/reshelf-sweep` Bearer-CRON_SECRET daily route. Admin `<AdminReshelfPanel>` at `/admin?tab=reshelf`: 3 stat tiles (High/Medium/Low counts), Show snoozed + Show dismissed toggles, per-row card with kind emoji + colored badge + severity pill (red HIGH / amber MED / grey LOW) + suite # + member + plan + suggested action + reasons inline chips + acted-note italic + snoozed-until amber timestamp + 3-action stack (✓ Did it with note prompt that auto-dismisses / ⏰ Snooze with day-count prompt / Dismiss with reason prompt). Wired into Operations nav as "Re-shelving". tsc clean exit=0.
98. **Member trust score on KYC docs** ✓ shipped iter-189 — composite 0-100 score per member computed from existing User columns + ForwardingAddress count, surfaces in admin compliance triage + per-member drilldown. Schema additions: `User.kycTrustScore` Int? + `kycTrustComputedAt` DateTime? + `kycTrustFlagsJson` for snapshotting flagged axes. Reusable `src/lib/kyc-trust.ts` (outside "use server"): `KycTrustBand` union (Trusted/Verified/Watch/At Risk thresholds at 85/65/40), `KycAxis` interface, `bandFor(score)` + `bandColor(band)` helpers. Server actions in `src/app/actions/kycTrustScore.ts`: private `computeForUserInternal()` evaluates 11 axes — `has_kyc_form` (+10), `has_primary_id_image` (+20), `has_secondary_id` (+15), `primary_id_type` (+10), `primary_id_number` (+10), `primary_id_not_expired` (+15/-25 with future-date check), `secondary_id_not_expired` (+5/-10), `kyc_status` (+20 when Approved), `has_phone` (+5), `email_well_formed` (+5 with regex), `has_forwarding` (+5 with at-least-one ForwardingAddress check). Raw range -35..+120 clamped to 0-100. Returns `KycTrustResult{score, band, axes[], flags[]}`. `getMemberKycTrustScore()` admin-side single-user; `getMyKycTrustScore()` member-side own; `recomputeUserKycTrust()` admin manual re-run + audit `kyc.trust_recomputed`; `runKycTrustSweep()` cron-callable walks all non-admin users + persists; `listLowTrustMembers({thresholdScore?, limit?})` admin queue (defaults to <65) with parsed flags array. New `/api/cron/kyc-trust-sweep` Bearer-CRON_SECRET nightly route. Admin `<AdminKycTrustPanel>` mounted at `/admin?tab=kyctrust`: 3 stat tiles (At risk <40 / Watch 40-64 / Never computed), threshold slider 0-100 with live filter, 2-col layout — left lists low-trust members (per-row big tabular score + colored band pill + name + suite # mono + plan + flag count + Drill button), right drilldown panel shows score + band + 11-axis breakdown table (✓/✕ + label + detail in parens + signed contribution mono colored green/red/grey) + flag chips with friendly emoji labels (📄 No Form 1583 / 🪪 No primary ID photo / ⚠️ Primary ID expired / etc) + "↻ Recompute" button. Wired into Compliance nav as "KYC Trust". tsc clean exit=0.
99. **Bureau revenue forecast widget** ✓ shipped iter-190 — combines two signals into a conservative 30/60/90-day revenue projection: trailing-90d realized revenue extrapolated forward (steady-state) + renewal pipeline summed with per-member confidence (auto-renew = 95%, manual = 65%). Final forecast = `max(trailingExtrap, pipelineCents × 0.85)` — conservative so admin doesn't over-spend on the projection. No schema changes — pure read against existing iter-89 `Payment` (status="COMPLETED" + syncedAt) + iter-83 `MailboxRenewal` (paidAt + amountCents) + `User` (planDueDate + planAutoRenew). Server action `getRevenueForecast({windowsDays?})` in `src/app/actions/revenueForecast.ts`: aggregates past-90d realized revenue across both Payment and MailboxRenewal, walks every non-cancelled User with planDueDate inside the largest forecast window, computes plan-implied amounts via `PLAN_MONTHLY_CENTS` × `planTerm` × confidence, returns `{windows[], windowsByDays{}, pipeline[], realizedLast90Cents}` with per-window confidence weight 0.55-0.95 biased higher when both signals agree (within 25%). Try/catch around each prisma aggregate so a schema mismatch never kills the whole forecast. Admin `<AdminRevenueForecastPanel>` mounted at `/admin?tab=forecast`: 3 big tiles for next 30/60/90d showing forecast $ + renewal-count subline + confidence %, detail breakdown table (Realized / Trailing extrap / Pipeline (n) / Forecast / Conf. % per window with green/amber/red color thresholds on confidence), upcoming-pipeline list sorted by planDueDate with mono date + member name + suite # + plan·term + green AUTO badge for auto-renewers + implied $ amount. ↻ Refresh button. Wired into Money & Comms nav as "Forecast". tsc clean exit=0.
100. **One-tap bulk renewal sweep** ✓ shipped iter-191 — admin-fired version of iter-111's `runAutoRenewSystemSweep` with configurable window (default 14d, picker chips for 7/14/30/60), preview-before-fire + post-fire result panel with per-row pass/fail. No schema changes — reuses iter-83 MailboxRenewal + iter-111 walletBalance + WalletTransaction primitives. Server actions in `src/app/actions/bulkRenewal.ts`: `previewBulkRenewal({windowDays})` walks `User.planAutoRenew=true` + planDueDate in [today, today+N], computes plan-implied charge, returns `{totalCandidates, willSucceed, insufficientWallet, noPlan, totalChargeIfRunCents, candidates[]}` with per-row blocker reason; `runBulkRenewal({windowDays, dryRun?})` processes serially (no Square thundering herd), per-row atomic `prisma.$transaction` of [User.update wallet+planDueDate, WalletTransaction Charge, MailboxRenewal create paymentMethod=Wallet + createdById, AuditLog `billing.bulk_renewal_charged`], fires `billing.auto_renewed` admin webhook per success; final roll-up audit `billing.bulk_renewal_swept`. Admin `<AdminBulkRenewalPanel>` at `/admin?tab=bulkrenew`: window picker chips with auto-refresh, 4 stat tiles, big green-gradient "🔁 Charge N now · $X.XX" CTA double-gated by `confirm()`, pre-fire candidate list with ✓/• pre-flight indicator + planDueDate + name + suite + plan·term + amount + blocker chip, post-fire result panel with green-or-amber 2px border + 3 outcome tiles + per-row ✓/✕ + outcome details. Wired into Money & Comms nav as "Bulk Renew". tsc clean exit=0.
101. **Customer self-serve forwarding-address book** ✓ shipped iter-192 — promotes iter-129's bare {label,address} CRUD into a real address book: per-row default star (exactly one ⭐ per user, atomic demote-siblings transaction, first-add auto-default, delete-default auto-promotes most-recently-used sibling so iter-170 batch sweeper always has a target), category enum (🏡 home / 🏢 work / 🌴 snowbird / 👪 family / 📦 storage / 📍 other), recipientName override (so packages addressed to sister-in-law don't get Karim's name), free-text notes (≤300 chars), `lastUsedAt` stamped by iter-170 batches and surfaced as relative timestamp ("used 3d ago"). Schema: extended `ForwardingAddress` with `recipientName String?`, `category String?`, `notes String?`, `isDefault Boolean @default(false)`, `lastUsedAt DateTime?`, `updatedAt DateTime @default(now()) @updatedAt` (default-now so existing rows backfill cleanly), plus `@@index([userId, isDefault])` and `@@index([userId, lastUsedAt])`. Server actions in `src/app/actions/forwardingAddressBook.ts`: `listMyForwardingAddresses()` (sorted isDefault desc, label asc, capped 30); `upsertMyForwardingAddress({id?, label, address, recipientName?, category?, notes?, isDefault?})` with full validation + atomic default-flag transaction (demote siblings → upsert) + first-address auto-default heuristic; `setMyForwardingAddressDefault({id})` atomic [updateMany demote, update promote, audit]; `deleteMyForwardingAddress({id})` with auto-promote-most-recently-used fallback inside the same transaction. Audit logs on every mutation: `forwarding_address.created/updated/default_changed/deleted` with metadata JSON. Member `<ForwardingAddressBookCard>` mounted in SettingsPanel above `<ScheduledForwardingCard>`: per-row card with tap-to-promote ☆/⭐ button, blue 2px highlight border on default, category chip, "used Nd ago" relative timestamp, recipientName 📛 line, multi-line address, notes 📝 line, edit/delete buttons; expanding inline form with nickname/address/recipientName/category-picker chips/notes/isDefault checkbox, ≤80/600/80/300-char client-side maxlengths matching server clamps. Empty-state dashed CTA. tsc clean exit=0.
102. **Mailbox color-tag system** ✓ shipped iter-193 — admin defines a small library of named colored tags ("VIP", "Compliance flag", "Translator-needed", "Snowbird-Q4", "High value", "Behind on rent", "Notary regular", "Watch list" ship as quick presets) and assigns any combination to suites; tags render as colored dots on every iter-117 occupancy heatmap cell + as full chips in the suite drilldown drawer for one-glance triage. Schema: new `MailboxTag {id, name @unique, color, description?, createdAt, createdById, updatedAt @default(now()) @updatedAt}` + `MailboxTagAssignment {id, userId, tagId, note?, createdAt, createdById, @@unique([userId,tagId])}` cascade-on-tag-delete + `[userId][tagId][createdAt]` indexes; User gains `mailboxTagAssignments` relation. Lib `src/lib/mailbox-tags.ts`: 9-color hex palette, `tagIsDark`/`tagChipStyle`/`tagSoftStyle` perceived-luminance helpers for contrast, `isHexColor` validator, 8 curated `TAG_PRESETS`. Server actions in `src/app/actions/mailboxTags.ts` (admin-only via verifySession role check): `listMailboxTags()` (with `_count.assignments`), `createMailboxTag/updateMailboxTag/deleteMailboxTag` (cascade-aware audit logs `mailbox_tag.created/updated/deleted` with assignmentsDropped count for delete), `assignMailboxTag({tagId,userId?|suiteNumber?|email?,note?})` with idempotent reapply (refreshes note instead of erroring on dup) + suite-or-email lookup heuristic, `unassignMailboxTag({assignmentId})`, `getUserMailboxTags`/`listMembersByTag` for drilldowns, `getAllUserTagAssignments()` returns `SuiteTagSummary[]` keyed by userId for one-shot heatmap render. Audit on every mutation: `mailbox_tag.{created,updated,deleted,assigned,unassigned}`. Admin `<AdminMailboxTagsPanel>` mounted at `/admin?tab=mailboxtags` (hidden nav under Operations): two-pane lg-grid `[1fr_360px]` — left library shows each tag as chip+desc+assignmentCount with edit/delete/assign↗ buttons, inline editor with 9-swatch color picker + 200-char description, "Quick presets" footer that lets admin one-click-seed any preset not yet defined; right pane shows selected tag + suite-or-email assign input + 200-char note input + member list with name+suite#+note+remove button. Wired into `<AdminSuiteOccupancyPanel>`: parallel-fetched `getAllUserTagAssignments` builds `Map<userId, SuiteTagSummary>` consumed by every cell render — top-right of cell shows up to 4 colored dots (5×5px each) + "+N" overflow, full chip strip with description+note tooltip in the drilldown drawer. tsc clean exit=0.
103. **Auto-translate inbound mail scans** ✓ shipped iter-194 — one-tap 🌐 Translate button on every scanned letter that OCRs the text with Claude Vision and translates it into the member's preferred language in a single round-trip (cuts latency + halves API bill vs separate OCR+translate calls). Schema: added `MailItem.translationJson String?` (one JSON blob per item — re-translating to a different language overwrites). Lib `src/lib/aiTranslation.ts` mirrors iter-108's aiAnalysis pattern (same `claude-haiku-4-5` model, same fetch-image-as-base64, same graceful no-op when ANTHROPIC_API_KEY unset, but 2048-token max + 25s timeout for letter-length output): `translateMailScan({imageUrl, targetLanguage?})` returns `{ok:true, sourceLanguage (ISO 639-1 detected), originalText (≤8000 chars), translatedText (≤8000 chars), language, model, promptVersion, translatedAtIso} | {ok:false, reason, attemptedAtIso}`. SUPPORTED_LANGUAGES list has 12 entries (en/es/fr/ar/zh/pt/de/it/ja/ko/ru/vi) with flag emojis. System prompt enforces: preserve names/addresses/dollar amounts/dates/account numbers verbatim, translate idioms naturally not word-for-word, return `originalText:""` if no readable letter. Server actions `src/app/actions/mailTranslation.ts`: `translateMyMailItem({mailItemId, targetLanguage?})` member-callable (verifySession, owner-gate via `userId !== session.id`), gates on `scanned && scanImageUrl` else returns "request a scan first" error, picks target lang from explicit input → `User.locale` (iter-183) → "en", **cache-hit shortcut** if `translationJson.language === target` returns instantly without re-calling model, audit `mail.translation_requested` only on success (with sourceLanguage + length + model in metadata) so failures don't spam audit log; `getMyMailItemTranslation({mailItemId})` read-only hydration helper for cached fetch. Member UI: new `<TranslationModal>` mounted in MailPanel, opens via new "Translate" ActionButton (only renders when item.scanned && item.scanImageUrl) — modal has language `<select>` with flag-prefixed labels, "Translate" / "↻ Re-translate" CTA, "✓ Cached" green badge when first-load hits cached blob, two-column original-vs-translated card layout with whitespace-pre-line for paragraph breaks, source-language detected chip (e.g. "detected FR"), red error card explaining `no_api_key` reason gracefully, footer disclaimer "machine translation, verify critical details". tsc clean exit=0.
104. **Bureau lobby digital signage** ✓ shipped iter-195 — public unauthed kiosk page at `/lobby` for a wall-mounted iPad: massive "NEXT UP · 10:30 AM" hero with member initials in 144px tabular-num pulsing type, three-column live grid (📦 Pickup queue · ✓ Checked in · 🚪 Tours today) auto-polled every 30s, slow-scrolling marquee footer auto-composed from open/closed status + appointment volume + admin-defined `SiteConfig.lobby_marquee` lines, sleek dark gradient backdrop (`#0B0F18 → #111729 → #1A1130`) so it doesn't burn in or hurt eyes after-hours, top-right pulsing OPEN/CLOSED pill that pulls from iter-90 `isOpenNow()` (handles closing-soon / lunch break / holiday states with distinct labels), live tabular-num clock + day-of-week. Server action `getLobbyBoardData()` in `src/app/actions/lobbyBoard.ts` is the one-shot read: pulls today's PickupAppointments (iter-101) bucketed into upcoming/checkedIn/completed with "isNext" flag for the future-or-just-past row; today's MailboxTours (iter-90) with confirmed/pending sub-status; iter-90 OperatingHoursConfig + isOpenNow result; bureau name from SiteConfig fallback "NOHO Mailbox". Privacy-tight: only initials surface ("K.S." not "Karim Saadi"), no phone/email, packageCount coarsened to "1 / 1–3 / ≤5 / 6+ packages" buckets, no full PII even though kiosk is unauthed. Per-row "DUE NOW" amber chip when scheduled within ±5min of clock; "LATE" red chip when >30min past and not yet checked in; "ready" green chip on checked-in rows. Marquee CSS keyframes for 60s linear roll + 1.6/2.4s pulse on hero initials and status dot. Wraps `<LobbyBoard>` client component in a `force-dynamic` host page so initial paint is fresh. Try/catch around every prisma read so a transient db hiccup doesn't blank the entire kiosk. tsc clean exit=0.
105. **Carrier insurance claim assistant** ✓ shipped iter-196 — admin "Start claim" wizard that aggregates declared value + intake/supplemental photos + tracking + storage timeline + iter-108 AI warnings into one paste-into-portal package per claim. Schema: new `InsuranceClaim {id, mailItemId, userId, carrier, claimType, declaredValueCents, claimedAmountCents, status (Draft|Filed|Paid|Denied|Closed), carrierClaimNumber?, evidenceJson, prefillJson, notes?, filedAt/filedById, paidAt/paidAmountCents, deniedAt/denialReason, closedAt, createdAt/createdById, updatedAt}` cascading off MailItem with `[userId][mailItemId][status][carrier,status][createdAt]` indexes; MailItem gains `insuranceClaims` reverse relation. Lib `src/lib/insurance-claim.ts` defines 6 carriers (USPS/UPS/FedEx/DHL/Amazon/Other) with portal landing-page URLs + carrier-specific filing windows (USPS 60d, UPS 60d, FedEx 21d, DHL 30d, Amazon 30d) so the wizard surfaces a "⚠️ EXCEEDED" warning when admin tries to file too late, 4 claim types (damaged/lost/missing_contents/other) with emoji labels, status color tokens, `describeClaim()` composes the carrier "describe issue" sentence, `evidenceBullets()` formats the timeline + tracking + photos + AI warnings into a copy-pasteable bullet list. Server actions `src/app/actions/insuranceClaim.ts` (admin-gated via verifyAdmin): `prefillClaimForMailItem` pure read aggregating iter-91 declared value + iter-93 publicShareToken + iter-94 tracking + iter-87 StorageThresholdAlert (threshold + feeAtAlertCents) + iter-108 aiAnalysisJson warnings + iter-135 extraPhotosJson gallery + iter-137 pickupSignature into one ClaimPrefill+ClaimEvidence pair (snapshotted into the row at start so the carrier sees what we sent, not what's current); `startInsuranceClaim` creates Draft + audit `insurance_claim.started`; `markClaimFiled` (with carrier claim # prompt), `markClaimPaid` (with paid amount), `markClaimDenied` (with denial reason), `closeClaim` — each runs an atomic `prisma.$transaction([update, audit `insurance_claim.{filed,paid,denied,closed}`])`; `listInsuranceClaims` / `getInsuranceClaim` / `lookupMailItemForClaim` (resolves by ID OR tracking #). Admin `<AdminInsuranceClaimsPanel>` mounted at `/admin?tab=insclaims` (hidden nav under Operations): 3 stat tiles (count + claimed $ + paid $), status filter chips, "+ Start claim" inline wizard with paste-anything lookup → live carrier+type chip pickers → live prefill preview with filing-window status, claim list with per-row status pill + carrier chip + claim-type emoji + amount + transition buttons (✓ Mark filed / 💸 Mark paid / Mark denied / Close), Open ↗ button opens a max-w-3xl scrollable modal with: filing-window-exceeded danger banner, 8 prefill fields each with 📋 copy button, evidence bullet block with "📋 Copy all", thumb grid of all photos linking to fullsize, "Open USPS portal ↗" button to the carrier's claims landing page in a new tab. tsc clean exit=0.

---

## Tier 14 — backbone enrichment + bureau-as-platform (#106-#115)

The 8 remaining open features for Tier 14, each one feature, each end-to-end, learning from prior iters and reusing audit/notification/email/webhook patterns.

106. **Member onboarding pickup-day reminder** ✓ shipped iter-197 — hourly `/api/cron/onboarding-pickup-reminder` cron self-checks whether we're inside the 3.5h–4.5h window before next bureau-open and no-ops outside it; when a member's FIRST-EVER package is currently Awaiting Pickup, fires a friendly SMS + branded HTML email with the open time + "bring photo ID" + suite # + map links. Skips members with a future PickupAppointment in next 48h (they've already committed to a slot — don't be redundant). Lib extension: `nextOpenDate(cfg, from)` in `src/lib/operating-hours.ts` walks forward up to 14d looking for the next non-holiday open day, computes the wall-clock-to-UTC instant via TZ-offset round-trip, returns null if bureau is misconfigured (closed all 14 days). Server action `runFirstPickupReminderSweep` in `src/app/actions/onboardingFirstPickup.ts`: pulls all `Awaiting Pickup` packages (capped 500), dedupes to oldest-per-user via Set, confirms it's actually their first-ever package via `count({type:Package, id:{not:c.id}}) === 0` (so a brand-new member's only package qualifies but a long-time member's "next" doesn't), idempotency via iter-148 OnboardingTouch unique key `userId_touchKey="first_pickup_reminder"`, fire-and-catch SMS via existing iter-84 sendSms + email via existing sendEmail with bespoke `firstPickupEmailHtml()` (warm cream backdrop matching brand book, hours / ID / suite # / today's hours grid, "Schedule a pickup time" + "Set up guest pickup" CTA links), per-member audit `onboarding.first_pickup_reminded` with mailItemId + opensAt + smsStatus + emailStatus in metadata. Returns `{scanned, fired, skippedNotInWindow, skippedHasAppointment, skippedAlreadyTouched, reminded[], windowStartIso, windowEndIso, openAtIso, minutesUntilOpen}` for cron-result observability. Cron route uses standard Bearer-CRON_SECRET pattern (matches all 23 sibling routes). Reuses iter-148 OnboardingTouch model — no schema changes. tsc clean exit=0.
107. **Auto carrier mismatch flag** ✓ shipped iter-198 — daily `/api/cron/carrier-mismatch-scan` (Bearer-CRON_SECRET) compares iter-94 admin-typed `MailItem.carrier` vs iter-108 Vision-detected `aiAnalysisJson.carrierGuess`; when ≥5 mismatches accumulate in the rolling 7d window AND we haven't coached in the past 6d, fires a warm-cream-themed "🪪 Intake quality alert" email to the admin with the top typed→AI pairs (e.g. UPS→USPS ×3) so admin sees the pattern. No schema changes — reuses iter-108 aiAnalysisJson + iter-148 audit pattern. Server actions in `src/app/actions/carrierMismatch.ts`: `listCarrierMismatches({windowDays})` returns `{rows[], summary{total, byTypedCarrier[], byAiCarrier[], pairs[], thresholdExceeded, threshold, lastCoachingSentIso}}` with all admin-correctable disagreement rows; `applyCarrierFix({mailItemId, newCarrier})` atomic update + audit `intake.carrier_corrected`; `dismissCarrierMismatch({mailItemId, reason?})` nulls out `aiAnalysisJson.carrierGuess` + stamps `carrierMismatchDismissedAtIso/By/Reason` so the row stops surfacing without losing the original AI analysis blob, audit `intake.carrier_mismatch_dismissed`; `runCarrierMismatchSweep()` cron-callable returns `{scanned, mismatches, thresholdExceeded, coachingSent, lastCoachingSentIso}`. Filter only counts strict disagreements (case-insensitive equality + "Other" excluded + AI must be one of the 5 recognized carriers — no false positives from Vision uncertainty). Admin `<AdminCarrierMismatchPanel>` mounted at `/admin?tab=carriermismatch` (hidden nav under Operations): window picker chips (7/14/30d), 3 stat tiles (mismatches / threshold / last coaching), threshold-exceeded danger banner, "Most-common typed→AI pairs" inline summary, per-row card with intake photo thumb + red-typed/green-AI carrier pills + tracking # + sender + member + intake date + ✓ Apply {AI carrier} (one-click correction with full audit) + "Typed is correct" dismiss button (with optional reason prompt). Coaching email lists top 6 mismatch pairs in a cream table with deep-link CTA to `/admin?tab=carriermismatch`. tsc clean exit=0.
108. **Receipt-roll consumption forecast** ✓ shipped iter-199 — pure-read extension of iter-150 supplies tracker that computes consumption velocity (units/day) over 7d/30d/90d windows for every active Supply, projects when on-hand will hit reorderAt and zero, and surfaces an actionable "🚨 Order ${N} {unit}s by ${date}" recommendation accounting for a configurable vendor lead time. No schema changes — reads `SupplyMovement.kind ∈ {sale, internal_use, loss}` (delta < 0) as consumption, ignores restock + adjust rows. Server action `getSupplyForecasts({leadTimeDays?})` in `src/app/actions/supplyForecast.ts` (admin-gated): per-supply walks 90d of negative-delta movements once, buckets into 7/30/90d sums, picks the most-reliable velocity window (prefers 30d → falls back to 7d for newer supplies → finally 90d), computes `daysUntilReorder = floor((onHand - reorderAt) / velocity)`, `daysUntilStockout = floor(onHand / velocity)`, urgency tier (`critical` when ≤ leadTime, `warning` when ≤ leadTime+14d, `ok` otherwise, `unknown` when no consumption history), `accelerationRatio = velocity7d/velocity30d` so admin spots Black-Friday-style surges (>1.5 = 🔥 surging) or post-holiday quieting (<0.5 = 🌬️ quieting), composes a per-row recommendation line that adapts to the urgency state. Returns `{rows[], summary{totalSupplies, critical, warning, ok, unknown, leadTimeDaysAssumed}}` sorted critical→ok then by daysUntilReorder asc. Admin `<AdminSupplyForecastPanel>` mounted at `/admin?tab=supplyforecast` (hidden nav under Operations): 4-tile row (Critical / Watch / Healthy / No history), lead-time slider 0-30d that re-computes urgency on the fly so admin can match their actual vendor turnaround (Amazon Business 1d vs Uline 5d vs custom-printed labels 14d), per-row card with urgency-tinted ring border + status pill + name + category + vendor + recommendation line + 6-pill metrics row (on-hand vs reorderAt + reorder qty + days-to-reorder + days-to-empty + reorder-by-date) + 3-window velocity readout with 🔥/🌬️ acceleration chip. tsc clean exit=0.
109. **Member referral landing pages** ✓ shipped iter-200 — public `/r/{code}` route auto-renders a personalized "Karim wants you to try NOHO Mailbox · $10 bonus" landing per iter-98 referral code; uses Next-15 force-dynamic + dynamic-metadata for shareable Open-Graph previews on iMessage / WhatsApp / Twitter. Schema extension on `Referral`: added `landingVisits Int @default(0)` + `lastLandingAt DateTime?` so admin can measure conversion rate per code without a separate visits table. Two PUBLIC server actions (no auth — code itself is the credential) in `src/app/actions/referralLanding.ts`: `lookupReferralLanding({code})` returns `{ok:true, code, referrerFirstName, referrerSuiteNumber, creditDollars, visitCount}` (privacy-light: only first name + suite #, no email/phone, so a malicious scraper learns nothing useful) or `{ok:false, reason: "not_found"|"claimed"}`; `recordReferralLandingVisit({code})` atomic increment + audit log `referral.landing_visited` with 60s throttle to dedupe React-StrictMode double-fires + fast-back-button spam. Page split: server component `page.tsx` does the lookup + generates the `<title>${name} invited you · $${credit} credit · NOHO Mailbox</title>` + description metadata for unfurl, falls back to graceful "code not found" card with sign-up-anyway CTA when invalid; client component `LandingClient.tsx` handles plan picker state + visit-bump on mount + brand-locked rendering. UI: cream-gold gradient backdrop, blue hero banner ("📬 NOHO Mailbox · $10 bonus when you join"), personalized headline with referrer's first name, big ${code} display in mono in a cream pill, 4-plan picker grid (virtual / basic / business / premium with sub + price labels) that flips brand-blue on selection, big red CTA button "Claim my $10 bonus →" linking to `/signup?ref={code}&plan={selected}` (existing signup already accepts ?ref=), bureau location card with address + hours + Open-in-Maps deep link, social-proof footer "🌟 N people visited this invitation. Be next." that uses the visit count to create urgency without lying. tsc clean exit=0.
110. **iCal feed for tour-bookings** ✓ shipped iter-201 — public `/api/cal/tours.ics` route emits a text/calendar feed of every iter-181 MailboxTour (today + future Pending/Confirmed plus 14d history of Completed/No Show for context, capped at 200) so admin can subscribe in Google Calendar / Apple Calendar / Outlook and see tours alongside personal events with auto-refresh ~15min. Auth: dual-mode mirroring iter-180's member iCal pattern — accepts either `?token=…` query param (recommended for calendar subscriptions since calendar apps can't send custom headers) or `Authorization: Bearer …` header (for tooling/curl). Token is a single `ADMIN_CAL_TOKEN` env var (constant-time compared via Node `timingSafeEqual` with equal-length buffer padding so length-mismatch and content-mismatch take identical time → no timing-side-channel leak), 503s with parseable plain-text error when env var unset, 401s with parseable hint when token wrong (calendar apps surface those errors usefully). Per-tour event: `🎉 Tour · Karim Saadi · party of 4 ⭐` (status glyph + name + party tag + ⭐ when becameMember=true), 30-min duration, description with status/source/party/reason/email/phone/admin notes, location pinned to bureau, deep-link URL to `/admin?tab=mailboxtours`. Reuses iter-180 `buildIcs` pure-TS RFC-5545 builder (no new lib code). Audit `tour_calendar.fetched` per request with event count + IP for "did Google Calendar refresh today?" observability. Server action `getTourCalendarSubscribeUrl()` (admin-gated) returns the full subscribe URL with token baked in — secret never touches the client bundle since it lives behind verifyAdmin and the env var is server-only. Admin UI extension: subscribe-URL widget added to top of `<AdminMailboxToursPanel>` with "Reveal subscribe URL" button → server action call → mono code block + 📋 Copy URL button (turns green-✓-Copied for 1.8s) + "Open in Calendar (webcal://)" deep-link that auto-launches the user's default calendar app. tsc clean exit=0.
111. **One-click "this is junk mail" rule learning** ✓ shipped iter-202 — replaces the "Block all mail from X?" confirm-prompt friction with a single-tap "🚫 Junk" that just reports the individual item; once 3+ reports stack up for the same normalized sender (no JunkSender rule yet), a yellow-amber dashboard card surfaces "Want to auto-discard from this sender going forward?" with a one-tap promote-to-rule that wires into the existing iter-149 JunkSender path. Schema: new `JunkReport {id, userId, mailItemId, senderRaw, normalizedSender (lowercased+trimmed for grouping), createdAt, promotedToRule}` cascading off both User and MailItem with `@@unique([userId, mailItemId])` (re-tapping is idempotent) + `[userId, normalizedSender]` and `[userId, promotedToRule, createdAt]` indexes for the bucketing query. Server actions in `src/app/actions/junkReports.ts`: `reportMailItemAsJunk({mailItemId})` upserts the report + flips MailItem.junkBlocked + audit `mail.junk_reported` + returns `{suggestPromotion, reportCount, normalizedSender}` so caller knows whether to celebrate the threshold; `getMyJunkSuggestions()` walks 90d of unpromoted reports, buckets by normalizedSender, suppresses senders already covered by an existing rule (prefix-match against JunkSender.sender), returns those with reportCount ≥ 3 sorted by frequency; `promoteJunkSuggestion({normalizedSender})` atomic [JunkSender.create with most-recent senderRaw as pattern (preserves capitalization member sees) capped to 80 chars, JunkReport.updateMany promotedToRule=true, MailItem.updateMany junkBlocked=true for matching from-string, audit `mail.junk_suggestion_promoted`]; `dismissJunkSuggestion({normalizedSender})` marks reports promoted=true without creating a rule (member said "don't suggest this again"), audit `mail.junk_suggestion_dismissed`. Member UI: MailPanel "Block" button rewired to call new lightweight `reportMailItemAsJunk` (no confirm dialog — just one tap), surfaces toast "Marked junk"; new `<JunkSuggestionsCard>` mounted at top of OverviewPanel below OnboardingChecklistCard renders ZERO when no suggestions, otherwise yellow-amber-tinted card with "Want to auto-discard from N senders going forward?" headline + per-sender row showing senderDisplay + Blocked Nx + first/last report dates + ✓ Auto-discard / Not now buttons. tsc clean exit=0.
112. **PDF claim-package export** ✓ shipped iter-203 — extends iter-196 InsuranceClaim modal with a green "📄 Export PDF" button that opens `/admin/print/insurance-claim/[id]` in a new tab; the print route renders a Letter-sized claim package (cover header, claim facts grid, sender/recipient block, description, evidence bullet list, 2-up photo grid, internal notes, footer with bureau address + carrier portal URL) optimized for the browser's "Save as PDF" destination so admin uploads ONE file to the carrier portal instead of cobbling together screenshots. Reuses iter-155's print-to-PDF pattern: server component (admin-gated via verifyAdmin) loads `getInsuranceClaim` via existing iter-196 action (no new server actions needed) + `<AutoPrint>` client component fires `window.print()` on mount with 350ms delay so the browser has time to load all evidence images before snapshotting them. CSS print stylesheet hides the on-screen "Print-to-PDF preview" banner + "Back to claims" button via `.print-hide`, sets `@page { size: Letter; margin: 0.55in }`, prevents image/section page-break-inside via standard CSS so each photo lands on one page. Status pill in cover header colors-by-status using same palette as iter-196 (Draft gray / Filed blue / Paid green / Denied red / Closed slate). Filing-window-exceeded value tinted danger-red. Each photo figure has its label as caption (e.g. "Intake photo (exterior)" / "Scanned contents" / "Additional intake angle") so the carrier reviewer knows what they're looking at. Footer stamps generation timestamp + claim ID for audit trail. Zero new dependencies, zero schema changes — pure print-stylesheet approach. tsc clean exit=0.
113. **Sender-side mail predictions** ✓ shipped iter-204 — pure-read sender-cadence forecaster that walks the member's last 180d of MailItems, groups by normalized sender, and predicts the NEXT arrival window per recurring sender via average inter-arrival gap + standard deviation. Member dashboard surfaces "based on your past mail, you'll get a package from Amazon in the next 7 days" so they can pre-stage forwarding for a trip / snowbird season instead of reacting after intake. No schema changes — works against existing MailItem rows. Server action `getMyMailPredictions()` in `src/app/actions/mailPredictions.ts`: filters out `junkBlocked` items (junk doesn't predict useful arrivals), buckets by lowercased+trimmed sender, requires ≥3 arrivals + ≤90d average cadence (anything sparser is too noisy), computes per-sender `{arrivalCount, avgCadenceDays, cadenceStdDevDays, lastArrivalIso, nextArrivalEstimateIso, daysUntilEstimate, windowStartIso/EndIso (estimate ±stddev), withinHorizon (≤14d), confidence, confidenceLabel: high|medium|low, category: package|letter|mixed}`. Confidence formula: `0.45*cadenceConsistency + 0.35*sampleScore + 0.20*recencyBoost` where consistency = `1 - (stddev/mean)` clamped, sample = `log10(N)/0.6` capped 1, recency = drop-off when last arrival exceeds 1 cadence (a sender that "should have arrived already" loses confidence). Returns top 6 sorted by within-horizon → confidence desc → soonest. Member `<MailPredictionsCard>` mounted in OverviewPanel below JunkSuggestionsCard renders ZERO when no predictions (new accounts), otherwise shows a card with "Based on your history, here's what's likely coming" headline + per-sender row with category emoji + confidence chip (green/amber/gray) + percent + senderDisplay (case-preserved) + "Expected today/tomorrow" or "in ~Nd" + arrival window range + "N arrivals in 180d · every ~Xd (±Yd)" subtext + "Pre-forward →" CTA deep-linking to `/dashboard?tab=settings` for forwarding-rule setup. Beyond-14d senders fold into a `<details>` "↓ N more recurring senders" disclosure to keep the card scannable. tsc clean exit=0.
114. **Bureau open-hours auto-publish to Google Business Profile** ✓ shipped iter-205 — daily `/api/cron/gbp-hours-publish` Bearer-CRON_SECRET cron pushes iter-90 OperatingHoursConfig (weekly + holidays) to the bureau's GBP listing via the My Business Business Information v1 API so customers searching Google see live hours and holiday closures without admin manually editing the GBP dashboard. No schema changes — pure read against existing SiteConfig + audit-log-driven idempotency. Lib `src/lib/gbp.ts`: env check (`checkGbpEnv` returns missing-vars list when GBP_OAUTH_CLIENT_ID/CLIENT_SECRET/REFRESH_TOKEN/ACCOUNT_ID/LOCATION_ID aren't all set), `buildGbpRegularHours(cfg)` translates the 7-row weekly array into GBP's `regularHours.periods[]` shape with proper `openDay/openTime/closeDay/closeTime` quads (splits around lunch breaks → 9:30-13:30 + 14:00-17:30 as two separate periods), `buildGbpSpecialHours(cfg)` translates holidays into `specialHours.specialHourPeriods[]` honoring `closed=true` (CLOSED day) + `openClose` (custom hours overriding default), `getGbpAccessToken({clientId,clientSecret,refreshToken})` posts to `oauth2.googleapis.com/token` with grant_type=refresh_token (15s timeout), `pushHoursToGbp({accessToken,accountId,locationId,regular,special})` PATCHes `mybusinessbusinessinformation.googleapis.com/v1/{accountPath}/{locationPath}?updateMask=regularHours,specialHours`. Direct fetch — no googleapis SDK dependency, matches the iter-108/iter-194 AI-libs pattern. Server actions in `src/app/actions/gbpHoursPublish.ts`: `runGbpHoursPublishSweep({force?, dryRun?})` cron-callable returns `{configured, missing, dryRun, pushed, unchanged, payloadHash, payloadPreview, error?, appliedAtIso, periodsRegular, periodsSpecial, lastPublishedAtIso}`, idempotent via SHA256 hash of `JSON.stringify({regular,special})` truncated to 16 chars compared against last successful publish's hash in metadata (skips push when unchanged → saves GBP API quota + audit-log noise); `previewGbpHoursPublish()` admin-gated dry-run; `forceGbpHoursPublish()` admin-gated force-republish that bypasses the unchanged-hash skip. Audit `gbp.hours_published` per successful push with hash + period counts + accountId + locationId. Admin `<AdminGbpHoursPanel>` mounted at `/admin?tab=gbphours` (hidden nav under System): 4-tile status row (Configured / Last publish / Regular periods / Special days), missing-env-vars amber banner with the exact `GBP_*` names admin needs to set + link to Google's OAuth docs context, payload preview block showing each weekly period as `Mon  09:30 → 13:30` font-mono lines + special-hours block with date + CLOSED/HH:MM-HH:MM, "🚀 Publish now" force button (disabled when not configured) + ↻ Refresh preview + payload hash chip with "unchanged since last push" annotation when applicable. Gracefully no-ops when env vars missing — preview still renders so admin can see exactly what would ship before configuring credentials. tsc clean exit=0.
115. **Member self-serve plan-pause** ✓ shipped iter-206 — true plan pause distinct from iter-99 vacation hold (which stops intake) and iter-179 downgrade (which is permanent): member pays a one-time prorated $5/mo holding fee, mail still arrives at suite during the pause, full subscription auto-resumes on the configured end-date OR member can early-resume anytime. Targets snowbirds (Oct–Mar in Tucson), travelers, sabbatical takers. Schema: new `PlanPause {id, userId, startDate, endDate, holdingFeeCents (snapshot at request), reason?, status (Scheduled|Active|Resumed|Cancelled), originalPlan/originalPlanTerm (snapshot for restore), activatedAt, resumedAt, resumedEarly, cancelledAt, cancelledReason, createdAt, createdById}` cascading off User with `[userId, status]` + `[status, startDate]` + `[status, endDate]` indexes for the cron sweeper. Server actions in `src/app/actions/planPause.ts`: `previewPlanPauseFee` returns `{feeCents, durationDays, months}` with min 14d (shorter trips → use vacation hold) + max 365d guards; `getMyPlanPause` returns the active or scheduled pause; `getMyPlanPauseHistory` returns last 10 closed; `requestPlanPause` snapshots originalPlan + creates Scheduled row + audit `plan_pause.requested` + sends warm-cream confirmation email; `cancelMyPlanPause` (Scheduled-only — Active routes to early-resume); `resumeMyPlanPauseEarly` atomic [PlanPause→Resumed with resumedEarly=true, User.plan→originalPlan, audit `plan_pause.resumed_early`]; `runPlanPauseSweep` cron-callable activates Scheduled→Active when startDate ≤ today (snapshots originalPlan as safety-net for legacy rows, sets User.plan="Paused" so renewal cron skips, fires "your plan is now paused" email) AND auto-resumes Active→Resumed when endDate ≤ today (restores originalPlan + originalPlanTerm, fires "welcome back!" email). Per-row idempotent via status filter — daily cadence is safe even at 5min intervals. Cron route `/api/cron/plan-pause-sweep` Bearer-CRON_SECRET. Audit per transition: `plan_pause.{requested,activated,resumed,resumed_early,cancelled}`. Member `<PlanPauseCard>` mounted in SettingsPanel below VacationHoldCard: form mode shows side-by-side date pickers + reason input + LIVE fee preview that recomputes on each date change ("$5.83 for 35d, ~1.2 months · billed at activation") with green/red banner for valid/invalid; active mode shows status pill (⏸ Currently paused / 📅 Scheduled), date range, days-until-resume countdown, restoring-to-PLAN line, holding fee + reason, ▶ Resume now button (green) + Cancel pause button (red). 3 bespoke warm-cream HTML emails (scheduled / activated / resumed) sent fire-and-forget so cron failures don't block the state transition. tsc clean exit=0.

---

## Tier 15 — bureau-as-network + member-as-multiplier (#116-#125)

The 10 features for Tier 15. Each ships one feature end-to-end, learning from prior iters and reusing audit/notification/email/webhook/cron patterns.

116. **Lobby selfie wall** ✓ shipped iter-207 — opt-in member showcase at the public `/wall` route + admin moderation. Rotating spotlight (8s cross-fade between approved photos) plus a 6-up grid below for "the whole crew" so visitors can scroll the entire community. Each spotlight card shows the member's display name in 32px black + suite # in a cream pill + "since YEAR" sticker in a brand-blue pill. Schema: new `LobbyWallEntry {id, userId, photoUrl, displayName, suiteNumber, joinedYear, consentedAt, status (Pending|Approved|Removed), reviewedById, reviewedAt, removedReason, createdAt, updatedAt}` cascading off User with `[status,createdAt]` + `[userId]` indexes; User gains `lobbyWallEntries` relation. Server actions in `src/app/actions/lobbyWall.ts`: member-side `getMyLobbyWallEntry` / `optInToLobbyWall({photoUrl, displayName?})` (validates https URL + ≤600 chars + auto-archives any prior Pending/Approved entry as "replaced_by_member" for clean history + snapshots suiteNumber + joinedYear from User.mailboxAssignedAt fallback createdAt + creates Pending row + audit `lobby_wall.opted_in`) / `revokeMyLobbyWallEntry` (flips own row to Removed with reason "revoked_by_member" + audit `lobby_wall.revoked`); admin-side `listAdminLobbyWallEntries({status})` / `approveLobbyWallEntry({id})` / `adminRemoveLobbyWallEntry({id, reason})` each gated by verifyAdmin + atomic update + audit `lobby_wall.{approved,removed}`; PUBLIC `getActiveLobbyWallEntries()` returns up to 60 Approved entries with privacy-tight projection (no userId/email/phone — only photoUrl + displayName + suiteNumber + joinedYear) for `/wall` route + iter-195 lobby kiosk loop. Member `<LobbyWallOptInCard>` mounted in SettingsPanel below PlanPauseCard: shows current entry status pill (✓ Live / ⏳ Awaiting review) with thumb + display name + "since YEAR", Update photo + Remove buttons; opt-in form has photo URL input + display-name override + required consent checkbox before Submit. Admin `<AdminLobbyWallPanel>` mounted at `/admin?tab=lobbywall` (hidden nav under Marketing): Pending/Approved/Removed/All filter chips, 2-up card grid with thumb + status pill + display name + suite # + joined year + submitted date + ✓ Approve / Remove buttons (Remove with optional reason prompt). Public `/wall` page: warm cream-gold gradient backdrop, header with "📬 NOHO Mailbox · The community wall · N members · since 2024", graceful empty-state when no Approved entries yet ("Wall's warming up"), CSS keyframe `wallFade` for 1s scale+opacity transition between spotlights, click-any-grid-cell jumps to that spotlight. Auto-revalidate `/wall` + `/lobby` paths on every mutation so iter-195 kiosk picks up new approvals within minutes. tsc clean exit=0.
117. **Bureau supply marketplace** ✓ shipped iter-208 — member-facing supply shop on the dashboard that browses iter-150 supplies + buys with iter-101 wallet at iter-164 Member-tier pricing (falls back to default tier when no Member tier exists, hides the row entirely when neither exists so members never see uncatalogued items). Same kind="sale" SupplyMovement row as admin-side sales (iter-150 path) → POS inventory + iter-164 margin reports stay coherent automatically. Server actions in `src/app/actions/memberSupplyShop.ts` (member-gated via verifySession): `listMemberSupplyShop()` returns active supplies with pickMemberTier resolution + `{priceCents, tierLabel, inStock}` per row; `buyMemberSupply({supplyId, quantity})` validates qty (1-50 sanity cap), in-stock, member-visible price exists, wallet ≥ totalCents (with friendly "Wallet has $X but this needs $Y. Top up first." error), then atomic 5-step `prisma.$transaction([Supply.update onHand, SupplyMovement.create kind="sale" with full price+cost+tier snapshot + customerId=userId, User.update walletBalanceCents, WalletTransaction.create kind="Charge" amountCents=-total with human description "Supply purchase: 2× Small box ($1.50/each)" → renders cleanly in iter-101 wallet history, AuditLog `supply.member_purchased` with full metadata]). Returns `{supplyName, qtyBought, totalChargedCents, newWalletBalanceCents, newOnHand}` so the UI can show a precise success toast. Member `<MemberSupplyShopCard>` mounted in OverviewPanel below MailPredictionsCard renders ZERO when bureau has no priced active supplies (clean for new bureaus / empty catalogs). Otherwise: category-grouped layout (📦 Boxes / 🩹 Tape / 🏷️ Labels / 📨 Poly mailers / ✉️ Envelopes / 🖨️ Printer ribbons / 🧰 Other) with per-row `{name, $price/unit, tier label, "N units in stock" (amber when ≤5, gray otherwise), notes}` + qty stepper input (1-50 clamp) + green "Buy · $total" button that confirms before charging. Out-of-stock rows render at 55% opacity with no buy controls. Footer disclaimer "Pickup at the bureau · payments go through your wallet · no shipping". No new schema — pure read+write against Supply + SupplyPriceTier + SupplyMovement + WalletTransaction. tsc clean exit=0.
118. **Mailbox neighbor introductions** — opt-in member directory at `/dashboard?tab=neighbors`: opted-in members see other opted-in members' first name + suite # + bio, can DM through iter-145 mailer threads. Builds the bureau's social graph.
119. **Suite-pin AR labels** — admin prints QR-coded suite pin labels (iter-141 SuitePinNote-style) that, when scanned by a phone in the lobby, overlay the member's NAME + last-pickup-date in AR via WebXR. Helps admin recognize suite-holders during in-person pickup.
120. **Door-camera OCR for walk-ins** — when iter-156 door access logs an unknown face, run iter-108 Vision against the camera frame to OCR any visible card/badge text + flag for admin review. Reduces front-desk identity-confirmation overhead.
121. **Postage-stamp QR receipt** — every paid USPS shipment ships with a 1×1in QR sticker the recipient scans to verify "this came from NOHO Mailbox · suite #X" → opens a member-facing certificate page. Builds brand recognition + trust.
122. **Snowbird forwarding playbook** — onboarding wizard for members entering plan pause (iter-206) that bundles forwarding address (iter-192 default) + auto-translate (iter-194 default lang) + recurring forwarding cadence (iter-170) into one "I'm leaving for the season" setup flow.
123. **Bureau alumni network** — when a member cancels, offer them a free "alumni" tier that keeps their email + Form 1583 archived for 5 years (CMRA min) + sends a quarterly "we miss you" newsletter (iter-152 pattern). Reactivation funnel.
124. **Multi-bureau federation** — for franchise expansion: SiteConfig adds a `bureauId` and the API exposes a federated `/api/bureau/{id}/...` namespace so a future "NOHO West Hollywood" can share schema + admin tooling.
125. **Member milestone badges** — auto-awarded badges for hitting milestones (1y member, 100 packages, $1000 lifetime spend, 10 referrals) surfaced on dashboard + iCal feed (iter-180) + member directory (iter-118 Tier 15). Drives long-tenure retention.
